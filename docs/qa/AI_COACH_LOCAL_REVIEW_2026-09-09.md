# مراجعة وإصلاح AI Coach — 9 سبتمبر 2026

## المصدر والنطاق

- المشروع: `G:\BIL_Project\worktrees\bil-unified-release-20260909`.
- الفرع: `codex/bil-unified-release-20260909`.
- المصدر: `8d48db48a9b8b5c59b6c10e8a26868bf590de13f`، مصدر مرشّحَي iOS 12 وAndroid 11.
- التعديلات فوق هذا المصدر نفسه، غير مثبتة في كوميت. لم يحدث بناء أو رفع أو تغيير في المتاجر أو السحاب ضمن هذه المهمة.
- النطاق: واجهة المدرب المحلية، ترتيب الرسائل والتمرير، استعادة السجل، بوابة الوصول، الانتظار والإلغاء وإعادة المحاولة، دورة حياة الصوت والكاميرا، الاستجابة للخط ولوحة المفاتيح، والاختبارات المرتبطة بذلك. ليست شهادة بخلو التطبيق كله من الأعطال.

## الأعطال التي عولجت

| المشكلة | التعديل والدليل المحلي |
| --- | --- |
| تحديث الرصيد يعيد إنشاء شاشة المدرب ويفقد موضع القراءة أو المسودة | الاحتفاظ بعنصر الشاشة أثناء تحديث الاستحقاق. اختبار على الصفحة والبوابة الفعليتين يتحقق من هوية الحالة والمسودة والتمرير. |
| ظهور ترحيبة جديدة أسفل المحادثة المستعادة | الترحيبة للمحادثة الفارغة فقط؛ السجل المستعاد ينتهي بآخر رسالة حقيقية. اختبار الإغلاق وإعادة الدخول. |
| انتقال الرسالة التي يقرأها المستخدم عند وصول رد | سجل ذو نقطة ارتكاز ثابتة؛ الاختبار يقارن موضع الرسالة على الشاشة قبل وصول الرد وبعده، وليس قيمة التمرير وحدها. |
| عدم اتساق الانتقال إلى أحدث رسالة | اعتماد نهاية السجل المحسوبة؛ الأحدث أسفل، والأقدم يصعد، ويمكن التمرير في الاتجاهين. اختبارات رد طويل ولوحة مفاتيح وزر العودة للأحدث. |
| تكرار فتح سجل المحادثات أو قائمة الأدوات بالضغط السريع | حماية من فتح أكثر من نسخة في الوقت نفسه، مع معالجة انتهاء القراءة بعد مغادرة الشاشة. |
| فقد محتوى محلي عند فشل استعادة السجل | عدم اعتبار الفشل محادثة فارغة، وعدم الكتابة فوق السجل التالف أو غير المقروء. تظهر إعادة المحاولة ويظل الرجوع متاحًا. اختبارات فشل القراءة وإعادة المحاولة وحفظ الأصل التالف دون استبداله. |
| رد قديم ملغى يتداخل مع الطلب التالي | عزل نتائج الطلبات بحسب رقم المحاولة؛ المؤقت القديم لا يلغي مؤقت المحاولة الجديدة. إعادة المحاولة لا تكرر رسالة المستخدم. |
| تشغيل الميكروفون أكثر من مرة أو بعد الخروج أثناء انتظار الإذن | تشغيل واحد مع إبطال النتائج المتأخرة وفحص حالة الشاشة والخلفية. اختبارات محاكاة لقنوات النظام. |
| ضياع مسودة مكتوبة عند تعذر التعرف على الكلام أو عند اكتمال رد صوتي | الاحتفاظ بالمسودة وعدم مسح النص الجديد المكتوب أثناء انتظار الرد. |
| استمرار الصوت أو إعادة تشغيل الاستماع بعد انتقال التطبيق للخلفية | إيقاف الإخراج الصوتي وتعليق المكالمة؛ لا يعاد الاستماع تلقائيًا بعد العودة دون متابعة المستخدم. |
| فتح الكاميرا مرتين أو فتحها بعد الرجوع بسبب تأخر الإذن | حجز محاولة الفتح قبل انتظار الإذن، وفحص بقاء الشاشة قبل فتح المسار. |
| ازدحام عنوان المدرب وأزراره مع الخط الكبير | تخطيط متجاوب، أهداف لمس للأزرار الأساسية 48، وقوائم قابلة للتمرير وحقل كتابة متعدد الأسطر يتكيف مع لوحة المفاتيح. |
| قراءة كل سجلات الوزن واليوم لحساب بصمة تغيير السياق | استعلامات تجميع صغيرة بدل تحميل كل الصفوف إلى Dart. اختبارات تطابق النتائج، بما فيها السجلات المحذوفة منطقيًا. هذا تقليل للعمل المحلي، وليس قياس FPS على هاتف. |
| وصف إذن التعرف على الكلام يوحي بمراجعة يدوية قبل الإرسال | صُحح النص العربي والإنجليزي ليوضح أن النص يرسل بعد توقف الكلام، بما يطابق سلوك الإدخال الصوتي الموجود. لم يتغير غرض الإذن أو نوع البيانات. |

نفاد الرصيد أو خطأ التحقق ما زال يمنع التفاعل؛ الاحتفاظ بالشاشة لا يمنح اشتراكًا ولا يتجاوز تفويض الخادم. تغيّر صاحب الاستحقاق ينشئ حالة واجهة جديدة، واختُبر عدم الاحتفاظ بمسودة الحساب السابق في تلك الحالة. هذا ليس اختبارًا شاملًا لعزل بيانات الحسابات في السحاب.

السطر `Searching your BIL context` ليس صف انتظار ظاهرًا في الواجهة الحالية. بقيت حالة الانتظار المدمجة في العنوان وزر إلغاء الطلب، ورسالة خطأ حقيقية مع إعادة المحاولة عند الحاجة.

## تنظيم الملفات

- `coach_anchored_history.dart`: التمرير ونقطة الارتكاز ودلالات ترتيب العناصر لقارئ الشاشة.
- `intelligence_conversation_viewport.dart`: تحويل الرسائل والبطاقة الافتتاحية وحالات الصوت/الفشل إلى عناصر السجل.
- اختبارات الصفحة الجديدة موزعة على ملف رئيسي وأجزاء مستقلة للعرض والوصول، والصوت، والطلبات والاستعادة؛ تستخدم صفحة الإنتاج مع بيانات وقنوات نظام محاكاة.
- لم تُحذف ملفات بيانات أو أرشيفات مستخدم. الصورة المرجعية المعدلة تخص واجهة المدرب وحدها.

## متطلبات Apple وAndroid التي استند إليها التحسين

- تكبير أهداف اللمس وتجنب تكديس الأزرار: [Apple UI Design Tips](https://developer.apple.com/design/tips/) و[Android accessibility](https://developer.android.com/guide/topics/ui/accessibility/apps). الأزرار الرئيسية جرى ضبطها إلى 48 وحدة منطقية؛ الحد الأدنى الموصى به لدى Apple هو 44pt ولدى Android هو 48dp.
- السماح بتكبير الخط وتكييف التخطيط بدل تعطيل إعداد المستخدم: [Apple Typography](https://developer.apple.com/design/human-interface-guidelines/typography). الاختبارات الهندسية تغطي العربية والإنجليزية، عرض 320 وارتفاع 700، وتكبير 200% مع لوحة مفاتيح محاكاة.
- فصل التمرير عن إضافة الرسائل، واستخدام نقطة ارتكاز وقيم دلالية للعناصر: [Flutter CustomScrollView](https://api.flutter.dev/flutter/widgets/CustomScrollView-class.html). التصميم المطبق تكييف محلي لهذه الآلية لسجل معكوس الاتجاه.
- الرجوع والتفاعل لا ينتظران اكتمال رد النموذج. حركة الانتقال للأحدث تحترم تعطيل الحركات، لكن لم يُنفّذ تدقيق VoiceOver أو TalkBack فعلي.

## نتائج التحقق

- التحليل البرمجي للميزة والبوابة ومستودعَي البيانات والاختبارات: `No issues found`.
- التشغيل النهائي على آخر التعديلات: **261 اختبارًا ناجحًا** للمدرب والتمرير وبوابة الوصول وسياسة الرصيد، دون فشل أو تخطٍ في هذه الحزمة؛ رمز الخروج `0`.
- **اختبار بصري إضافي واحد ناجح**: الصورة المرجعية رُسمت من صفحة الإنتاج باستخدام خطوطها، وفُحصت بصريًا، ثم نجحت المقارنة دون `--update-goldens` ودون تعطيل مقارنة البكسلات؛ رمز الخروج `0`. المجموع في التحقق النهائي: 262 اختبارًا.
- فحص فرق Git للتنسيق ناجح مع مراعاة ملفات CRLF الموجودة. لم يُشغّل فحص المشروع الكامل، ولم يُثبّت كوميت.
- أخفقت اختبارات قديمة بسبب افتراض `ListView` أو توقيع دالة قديم؛ عُدلت لتتبع الواجهة الحالية مع إبقاء التحقق من وجهات التنقل ومنع إرسال الصوت الخام. وصُحح اختبار نص الإذن ليطابق الإرسال الصوتي الفعلي.

الأوامر المستخدمة من جذر المشروع، بتشغيل متسلسل:

```powershell
flutter analyze --no-pub lib/features/intelligence_center lib/features/commerce/presentation/premium_route_glass_gate.dart lib/data/repositories/weight_repository.dart lib/data/repositories/daily_log_repository.dart test/features/intelligence_center

flutter test --no-pub --concurrency=1 --reporter expanded test/features/intelligence_center test/shared/widgets/chat_history_viewport_regression_test.dart test/features/commerce/premium_ai_market_gate_widget_test.dart test/features/commerce/ai_coach_live_refresh_contract_test.dart test/features/commerce/ai_coach_credit_access_policy_test.dart

flutter test --no-pub --concurrency=1 --reporter expanded test/visual_closure/actual_data_pages_golden_test.dart --plain-name 'AI Coach conversation production phone capture'
```

## ما بقي بالضبط — دون ادعاء أنه اختُبر

### تحقق على الأجهزة قبل اعتبار الإصدار جاهزًا

1. iPhone وAndroid: سجل طويل، كتابة مع لوحة المفاتيح، قراءة رسالة قديمة أثناء وصول رد جديد، الإلغاء وإعادة المحاولة، فتح السجل والرجوع، وتكبير خط النظام. الاختبارات المحلية لا تشغّل لوحة مفاتيح أو إيماءات نظام حقيقية.
2. إذن الميكروفون والتعرف على الكلام والكاميرا: رفض الإذن ثم تفعيله، تبديل الخلفية والقفل، مكالمة واردة، وسماعة Bluetooth. الاختبارات الحالية تحاكي قنوات النظام ولا تسجّل صوتًا أو صورة من جهاز فعلي.
3. رصيد وموافقة وخطأ شبكة حقيقية بحساب اختبار مصرح: اختبارات البوابة محلية ولا تثبت حالة الاشتراكات أو جودة رد النموذج في الإنتاج.
4. قياس أداء Profile على الأجهزة وقراءة VoiceOver/TalkBack. لم أقس FPS أو أعلن انتهاء اللاج العام للتطبيق.

### حد وظيفي معروف، وتحسينات منفصلة

- التعرف الصوتي في iOS يبدأ حاليًا بلغة النظام عندما لا تُرسل لغة محددة إلى `BILSpeechBridge.swift`. لا يوجد هنا إثبات لتبديل تلقائي موثوق بين 25 لغة. اختيار لغة التعرف صراحةً عند الحاجة تحسين واضح لم يُضف في هذه المهمة. مسار Android يطلب التبديل من النظام على Android 14+ عند دعمه؛ توفره الفعلي يعتمد على محرك الجهاز.
- سجل المحادثات المعروض محلي. بحث داخل السجل، وتصديره، ومزامنة المحادثة بين الأجهزة ميزات مستقلة غير منفذة هنا، وليست حلولًا مطلوبة لتثبيت التمرير. المزامنة بين الأجهزة تحتاج قرار خصوصية وتصميم بيانات، وليست تعديل واجهة بسيطًا.
- إعدادات الرصيد والموافقة، التقييم والإبلاغ، وتحليل الطعام موجودة في الكود؛ نجاح عقودها المحلية ليس إثباتًا لتسليم كل عملية في السحاب. لم تُجر معاملات شراء أو بلاغات أو تغييرات أهداف على حساب حقيقي.
- لم تتغير صلاحيات النظام أو المتاجر أو نشر السياسات. النص المصحح داخل حوار الإذن لا يعني أن صفحات المتاجر أو الدومين عُدّلت.

## التسليم

الإصلاحات محلية فوق مصدر الإصدار المحدد، وليست داخل نسخة TestFlight المثبتة بعد. لا كوميت ولا رفع ولا بناء ضمن هذه المهمة. الشجرة تحتوي هذه التعديلات غير المثبتة عمدًا، مع الحفاظ على المصدر السابق.

## التشخيص السابق للصور الثماني — قبل طلب الإصلاح

بعد سؤال المستخدم عن اللغات ومسح بطاقة المحادثة وزر «ساعدني أختار»، شُغّلت ست حالات تشخيص محلية في `build/diagnostics/coach_followup_review_20260909_test.dart`. نجحت في إعادة إنتاج السلوكيات أدناه؛ نجاح التشخيص لا يعني أنها السلوكيات المطلوبة. لم يتغير كود الإنتاج في هذه المتابعة.

1. **اللغات ليست مغلقة بالكامل:** عناوين وأزرار الكاميرا المختارة في مصفوفة الفحص تُترجم في اللغات الـ25، لكن رسائل خطأ تحليل الصورة وتنبيه المراجعة الافتراضي ترجع إلى الإنجليزية في 20 لغة. كما يعرض حوار المراجعة الاسم والوصف والوحدة والتنبيه القادمين من الخدمة كما هي؛ أعاد الاختبار إظهار `tomato` و`piece` والتنبيه الإنجليزي داخل الحوار العربي. طلب `requested_locale` موجود، لكنه وحده لا يضمن لغة النص المعروض. ونص إذن التعرف على الكلام الذي صُحح في التعديل السابق ما زالت تنقصه 23 ترجمة غير العربية والإنجليزية.
2. **مسح المحادثة لا يحذف بطاقتها من السجل:** أعاد اختبار الصفحة الفعلية إنتاج اختفاء الدردشة الظاهرة مع بقاء مدخل المحادثة نفسه في الأرشيف وقائمة المحادثات. الدالة `_clearConversation` تمرر `historyArchive.conversations` كاملة دون استبعاد المحادثة الحالية. المطلوب فصل «محادثة جديدة مع الاحتفاظ بالأرشيف» عن «حذف هذه المحادثة ورسائلها وبطاقتها»، مع حماية بقية المحادثات وعدم إظهار نجاح عند فشل الحفظ.
3. **زر «ساعدني أختار» موصول، لكن مسار فهم الطلب غير صحيح:** بالضغط في العربية والإنجليزية تُضاف رسالة المستخدم ويبقى المسار `/intelligence-center`. إلا أن الرد في بيانات الاختبار كان هدف البروتين 142غ، لا اقتراح مصدر بروتين. `_answerDailyTarget` يلتقط كلمة `protein` أو `بروتين` قبل وصول طلب الاختيار إلى مسار التوصية. المسار الأنسب هو طلب إرشاد داخل المحادثة، يميّز اختيار الطعام عن الاستعلام عن الهدف، ثم يقدم خيارات قابلة للفتح في الطعام عند الحاجة.

الواجهة وتنبيه الكاميرا يفترض أن يتبعا لغة التطبيق. لغة جواب المحادثة حاليًا تتبع لغة سؤال المستخدم عمدًا؛ لذلك الرد العربي على سؤال عربي داخل واجهة إنجليزية ليس، بمفرده، سقوط ترجمة. أسماء المستخدمين والعلامات مثل BIL وGemini لا ينبغي تغييرها باعتبارها أخطاء ترجمة.

بالنسبة للومضة القديمة: الاختبار السابق على مصدر الإصدار أعاد إنتاج إزالة الشاشة وإعادة إنشائها بعد تحديث الرصيد، وهذا عولج في التعديلات المحلية. لا يُعد ذلك وحده إثباتًا لسبب كل ومضة على هاتف المستخدم. كانت الحالات الثلاث تحتاج الإصلاح الإضافي المنفذ أدناه.

## إصلاح متابعة اللغات والمسح والاقتراحات — 2026-09-09

نُفذت بطلب «اصلحها بدقة متناهية» على الشجرة نفسها، وما زال HEAD هو `8d48db48a9b8b5c59b6c10e8a26868bf590de13f`. لا staging أو commit أو بناء أو نشر في هذه المتابعة.

### ما تغير

- **المسح:** تأكيد واضح بأن العملية تحذف المحادثة وبطاقتها؛ حذف الهوية الحالية وحدها مع الرسائل النشطة في معاملة محلية واحدة. المحادثات الأخرى تبقى، وفتح سجل المحادثات لا يعيد إنشاء المحذوفة. يعطّل الإدخال أثناء العملية ويوقف تأثير الردود المتأخرة. إيقاف الصوت لا يُؤخّر الحذف بانتظار استجابة النظام. فشل الكتابة لا يمسح المحادثة من الشاشة، ويُبقي بياناتها ويتيح إعادة المحاولة. قراءة أرشيف تالف لا تتحول إلى حذف صامت لبقية السجل.
- **ساعدني أختار:** قُصر اختصار هدف البروتين/السعرات على الاستعلام المباشر عن الهدف. طلب اختيار الطعام وأسئلة المتبقي والمقارنات تمر لمسار الإجابة مع سياق اليوم، ولا تعود بالهدف الكامل بدل الاقتراح. يبقى المستخدم داخل المحادثة، ولا تُسجل وجبة أو تُعدل خطة تلقائيًا. قواعد الموافقة والرصيد والشبكة الموجودة لم تُتجاوز.
- **الترجمة الثابتة:** أُضيف كتالوج للنصوص المتأثرة عبر اللغات الـ25، بما فيها أخطاء الصور، تنبيه المراجعة، معنى المسح، ومبررات وأخطاء أذونات الصوت/الكاميرا المستخدمة. صُحح بناء عنوان الإذن؛ لم يعد يُركَّب اسم مترجم داخل مفتاح إنجليزي قبل البحث عن الترجمة. مكونات البرتغالية والصينية تبقى منفصلة.
- **نتيجة الكاميرا:** يملك التطبيق تنبيه المراجعة المترجم، فلا تستبدله جملة إنجليزية من الخدمة. وحدة `piece` تُعرض مترجمة وتعود إلى مفتاحها الحسابي عند التأكيد. لا تُغير ترجمة الوحدة الوزن أو الكمية.
- **لغة بيانات النموذج:** تعليمات مزود الصورة تطلب لغة التطبيق لكل الاسم والدليل والبدائل والتحذيرات وعدم اليقين والمكونات المرئية، مع `response_locale` صريح. تُرفض إيصالات لغة مختلفة أو ناقصة للنتائج غير الإنجليزية، وتُفحص الكتابة للكشف عن اختلافات واضحة مثل وصف إنجليزي تحت إيصال عربي. مفاتيح ذاكرة الصور تشمل اللغة كي لا تُعاد صورة بنفس نتيجة لغة قديمة. فشل عقد اللغة لا يؤدي إلى إعادة إرسال تلقائي إضافي لنفس التحليل.

### التحقق الحالي

- **411 اختبار Flutter ناجحًا**، دون فشل أو تخطٍ، في تشغيل متسلسل واحد: حزمة AI Coach السابقة كاملة، التمرير وبوابات الرصيد، الاختبارات الجديدة للمسح والاقتراحات، وعقود الصور واللغات والمراجعة والحصص. رمز الخروج `0`.
- منها **37 حالة جديدة** للمسح والاقتراحات، و**79 حالة جديدة** لترجمة الصور ومبررات الإذن والنتائج، تشمل الضغط الفعلي على نافذة المراجعة العربية وإعادة كمية بوحدة حسابية سليمة.
- **38 اختبار Deno ناجحًا** لمزودي الصور وعقد اللغة وذاكرة النتائج؛ اختبار الطلب/النتيجة يشمل المزودين الثلاثة واللغات الـ25. `deno check supabase/functions/analyze-meal/index.ts` ناجح.
- التحليل البرمجي لجميع الملفات المتأثرة واختباراتها: **No issues found**. مقارنة صورة AI Coach المرجعية من صفحة الإنتاج نجحت دون تحديث الصورة. الإجمالي الحالي **450 اختبارًا ناجحًا: 411 + 1 بصري + 38 للخدمة**.
- اختبارات فشل الحذف والرد المتأخر وإعادة الفتح تستخدم صفحة الإنتاج وقاعدة محلية في الذاكرة. نتائج النموذج محاكاة مضبوطة، لا استدعاءات مدفوعة ولا صور مستخدم مرسلة خارجيًا.
- لم تُخفَ إخفاقات أثناء التطوير: صُححت تهيئة الاختبارات (انتظار بناء الصفحة وتطبيع نص الطلب)، وعولج انتظار إلغاء الصوت أثناء الحذف. النتائج أعلاه تخص إعادة التشغيل الناجحة بعد التصحيح.

أُجريت اختبارات وظائف الخدمة وفق [إرشادات Supabase لاختبار Edge Functions](https://supabase.com/docs/guides/functions/unit-test)، دون تعديل قاعدة الإنتاج أو صلاحياتها.

### شرط إصدار مهم وحدود الإثبات

1. **شرط الخدمة أُنجز في المتابعة أدناه:** نُشر تحديث `analyze-meal` مع تبعياته بالإصدار 58 قبل توزيع أي نسخة تطبيق جديدة. العميل الجديد يرفض نتيجة غير إنجليزية بلا إيصال اللغة الجديد. بوابة `legacy_gateway` إن استُخدمت مستقبلًا يجب أن تعيد `response_locale` والنصوص باللغة المطلوبة؛ آخر إيصالات الإنتاج قبل النشر كانت ناجحة باستخدام Gemini مباشرة.
2. تغطية الكتالوج تمنع fallback للنصوص الثابتة التي عُدلت. إيصال اللغة وفحص الكتابة ليسا مترجمًا أو كاشفًا لغويًا كاملًا؛ لا يثبتان جودة كل جواب مولَّد أو يمنعان كل خلط ممكن بين لغات تشترك في الحروف. لا ندّعي ضمانًا لغويًا 100% لنموذج حي لم يُختبر.
3. الردود القديمة محفوظة بلغتها الأصلية، ولغة الرد على سؤال مكتوب تتبع السؤال عمدًا. أسماء الأشخاص والعلامات لا تُترجم. ولم يتغير حد التعرف الصوتي الأصلي على الأجهزة المذكور سابقًا.
4. يلزم تحقق قصير على iPhone وAndroid بعد نشر الخدمة وبناء النسخة؛ الاختبارات المحلية لا تعني أن TestFlight المثبتة تغيرت.

## متابعة النشر واختيار المحادثة والصور — 9 سبتمبر 2026

بناءً على طلب المستخدم الصريح نُشرت خدمة الصور فقط؛ لم يُنفذ commit أو push أو بناء/رفع تطبيق جديد. كل تعديلات الواجهة السابقة والجديدة ما زالت على `codex/bil-unified-release-20260909` فوق `8d48db48a9b8b5c59b6c10e8a26868bf590de13f`.

### سلوك الواجهة

- «مسح المحادثة» يفتح قائمة المحادثات لاختيار المحادثة المقصودة. بعد الاختيار يظهر عنوانها وتأكيد الحذف. المحادثة المختارة ورسائلها وبطاقتها تُحذف فقط؛ بقية المحادثات تبقى. فتح القائمة وإلغاؤها لا يغيّر الأرشيف.
- حذف محادثة غير مفتوحة لا يغيّر المحادثة الحالية أو المسودة أو يوقف ردها الجاري. حذف المحادثة الحالية يبطل ردودها المتأخرة وحفظها المعلّق. القراءة المتسامحة للأرشيف لا تُستخدم لاستبدال بيانات تالفة أثناء الحذف.
- زر الكاميرا يعرض «التقط صورة» أو «اختر من الجهاز» أو «إلغاء»، بترجمات اللغات الـ25. التصوير يبقى داخل التطبيق؛ اختيار صورة يستخدم منتقي النظام مع `requestFullMetadata: false` وتصغير 2048 بكسل وجودة 88، ثم نفس خدمة التحليل ومراجعة الأطعمة.
- لا يُطلب إذن الكاميرا لاختيار صورة من الجهاز. فُصل قفل العملية الواحدة عن مؤشر التحليل؛ لا يدور مؤشر تحليل أثناء انتظار اختيار المستخدم أو تأكيد المراجعة. الإلغاء لا يرسل صورة ولا ينشئ محادثة، والنتيجة المتأخرة بعد مغادرة المدرب تُهمل.
- استرجاع الصور عند قتل نشاط أندرويد مرتبط بغرض مستقل `coachFoodPhoto` وبصاحب الحساب. لا يُحوّل الصورة إلى الوجبات أو الملف الشخصي. تبقى الصورة المسترجعة للاختيار الصريح من مسار المدرب نفسه؛ لا تُحلل تلقائيًا عند بدء التطبيق.

تنفيذ اختيار الصور يستند إلى [توثيق image_picker الرسمي](https://pub.dev/packages/image_picker)، بما فيه استرجاع البيانات على أندرويد ومنتقي صور iOS وعدم طلب البيانات الوصفية الكاملة. مفاتيح إذن مكتبة الصور في iOS موجودة، وAndroid يستخدم `singleTop` لا `singleInstance`.

### إثبات النشر

- المشروع: `tgmanzhqulksykhslrzb`، مطابق لعناوين خدمة الصور في مساري بناء iOS وAndroid.
- الوظيفة: `analyze-meal`، **ACTIVE v58**، مع الحفاظ على **`verify_jwt: true`**. وقت النشر: **9 سبتمبر 2026، 17:43:14 UTC / 20:43:14 بتوقيت مصر**.
- بصمة حزمة النشر: `828c72af3549cf6956611d759e05fbb0fba8008863d9e3ca935b3b902c6313ad`.
- نُزّلت ملفات الإصدار المنشور بعد النشر؛ الملفات الثمانية والتبعيات النسبية مطابقة للكود المحلي. إصدارات بقية وظائف المشروع لم تتغير.
- فحص حي: `OPTIONS` يعيد **200 / ok**؛ طلب `POST` بلا اعتماد يعيد **401**. لم تُجرَ مكالمة تحليل صورة مدفوعة، ولا ندّعي اختبار نموذج حي بكل اللغات.
- حُفظت نسخة وظيفة v57 قبل الاستبدال ونتيجة التحقق محليًا داخل `build/diagnostics/coach_publish_20260909/`. لم تتغير مفاتيح الإنتاج أو إعداداته أو مخطط قاعدة البيانات أو RLS.
- قبل النشر نجحت اختبارات Deno الـ38 وفحص `deno check` لملف الدخول. أُخذت طريقة التحقق والحفاظ على JWT من إرشادات Supabase؛ هذا النشر لا يغيّر تطبيق TestFlight المثبت.

### شفافية التشغيل المحلي

لم تُعتبر الجولات غير المكتملة نجاحًا: صُحح تعارض اسم متغير وتجهيز بيانات اختبار الصورة، ثم استُبدل انتظار كل الحركات في اختبارات الرد المعلّق بانتظار إطارات محدد حتى لا ينتهي توقيت الطلب داخل الاختبار. كشف اختبار اختيار الصور أيضًا دوران مؤشر التحليل قبل التحليل الفعلي؛ فُصل مؤشر التحليل عن قفل اختيار الصورة. تعطل تشغيل لاحق بسبب Windows `errno = 1450` (نقص موارد النظام)، ثم نجح اختبار حذف المحادثة غير النشطة منفردًا. سجل التعطل منفصل عن سجل إعادة التشغيل النهائية.

### النتيجة النهائية لهذه المتابعة

- **453 اختبار Flutter موجّهًا: كلها ناجحة**، بلا فشل أو تخطٍّ، في تشغيل تسلسلي واحد. تشمل المدرب والتنقل والصوت والتمرير والحذف واللغات ومراجعة الصور والرصيد واسترجاع الصور على أندرويد، وليست فحصًا شاملًا للمشروع.
- **38 اختبار Deno: كلها ناجحة**، مع نجاح فحص أنواع ملف دخول الوظيفة قبل النشر.
- **اختبار بصري واحد ناجح** لصفحة محادثة المدرب الفعلية، دون `--update-goldens`. أُعيدت معه 37 حالة للحذف واختيار الصور بعد ضبط تنسيق الأقواس، ونجحت كلها؛ لا تُحسب التكرارات مرة ثانية.
- المحصلة **492 حالة اختبار مختلفة ناجحة** (453 + 38 + 1). التحليل النهائي للأجزاء المعدّلة واختباراتها: **No issues found**. استُخدم `dart analyze` بعد تعليق مشغّل `flutter analyze` قبل بدء التحليل، وصُححت ملاحظات الأقواس الثمانية قبل إعادة التحليل النظيفة.
- `git diff --check` ناجح مع اعتبار CRLF، وHEAD ما زال `8d48db48a9b8b5c59b6c10e8a26868bf590de13f`، والفهرس فارغ من staged changes.
- أدلة التشغيل: `build/diagnostics/coach_publish_20260909/final-focused-tests.log` و`final-analysis.log` و`final-visual-and-actions.log` و`deployment-verification.json`.

**حد التسليم:** خدمة الصور منشورة فعليًا؛ تغييرات الواجهة مكتملة ومختبرة محليًا على الشجرة نفسها، لكنها تحتاج بناء تطبيق جديد حتى تظهر في الهاتف. لم يُنفّذ commit أو push أو إنشاء/رفع نسخة iOS أو Android في هذه المتابعة، ولم يُجرَ اختبار على جهاز مادي أو استدعاء مدفوع للنموذج.
