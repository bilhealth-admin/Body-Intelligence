# نشر الاشتراكات الإدارية — 2026-09-10

## النتيجة والنطاق

بناءً على طلب المالك «انشر»، نُشر جزء الخادم الخاص بالاشتراكات الإدارية فقط على مشروع Supabase `tgmanzhqulksykhslrzb` (`body-intelligence-log`، eu-west-1). لم يُبن التطبيق، ولم يُنفذ commit أو CI أو رفع للمتاجر. لم يُنشأ اشتراك إداري أو يُلغَ اشتراك لحساب حقيقي، ولم تُرسل إشعارات أو يُنفذ reset للاختبار.

- migration: `20260910014118_admin_subscription_grants`، نجاح التطبيق ثم ظهورها في سجل الإنتاج. أصبح السجل 125 ترحيلًا بدل 124.
- Edge Function: `ai-coach-global-reset`، الإصدار 18 → **19**، الحالة **ACTIVE**، مع بقاء **verify_jwt=true**. وقت تحديث الخدمة: `2026-09-10T01:41:31.399Z`.
- معرّف الخدمة بقي `bbc5f7bb-88e5-4cf0-a210-8cbbe38f8ab2`؛ بصمة الحزمة الجديدة `aa815f635b11b6c75c011d3fdfa4a3a9e8f9c2bae11ab6d0091f0c8a2832fe63`.
- لم تُنشر خدمة أخرى أو migration أخرى، ولم تُغيّر أسرار سلامة الجهاز أو إعدادات Auth أو سياسات Community/Social v2.

## مطابقة الحالة وحفظ المصدر

المجلد المحلي للأدلة: `build/diagnostics/admin_console_deploy_20260910_0136/`.

قبل النشر: طابق سجل الإنتاج جميع ملفات migrations السابقة، ولم يكن `bil_admin_private` أو RPC المنحة موجودًا. حُفظ كود الخدمة الحي وmetadata وسجل migrations وفحص الأمان وتعريفات تسع دوال بصلاحياتها ومالكيها في `before/`. فرق الخادم المحلي محدود بإضافة عمليات list/grant/revoke إلى خدمة الإدارة القائمة؛ index وملف سلامة الجهاز مطابقان للنسخة الحية.

استخدم النشر migration واحدة عبر واجهة الترحيلات، لا `db push`. أعطت الواجهة رقم النشر `20260910014118`، ولذلك أعيدت تسمية الملف المحلي من رقم التحضير `20260910005439` وحدّث مسار الاختبار فقط. SQL المنشور محفوظ قبل إعادة التسمية، ولم يتغير محتواه.

اتّباع مهارة Supabase أبقى التحقق من الانحراف، والمقارنة قبل/بعد، وفحص الصلاحيات ضمن إجراءات النشر. روجع أيضًا [تغيير التعريض التلقائي لـData API](https://supabase.com/changelog/45329-breaking-change-tables-not-exposed-to-data-and-graphql-api-automatically)؛ الصلاحيات الجديدة صريحة ولا تعتمد على منح افتراضية.

## ما تحقق فعليًا

- أعيد تشغيل 21 فحص PostgreSQL محلي داخل PGlite قبل النشر، ثم بعد مطابقة اسم migration. لا اتصال إنتاجي في هذا الاختبار. تشمل حالات المنح والإلغاء والانتهاء والحصة الفعلية، بقاء مشتريات Apple/Google وBoost، ومنع إعادة تعبئة الاستهلاك عند إعادة المنح.
- أعيد تشغيل 40 اختبار Deno/type-check في ملفات server (26)، subscription_operations (9)، mobile_integrity (5)، وكلها ناجحة. هذه جولة محددة، وليست جمعًا مع الجولة السابقة ذات 41 اختبارًا.
- استرجع كود Edge المنشور، وطابق كل ملفات التنفيذ الأربعة بعد توحيد نهايات الأسطر فقط. تضمنت حزمة الطلب أيضًا `supabase/functions/deno.json`، ولا يعيده مزود الاسترجاع ضمن ملفات التنفيذ.
- نجح `OPTIONS` حيًا برمز 204؛ رُفض POST لعملية القائمة بلا Authorization برمز 401. لم يُتجاوز حاجز JWT للاختبار.
- نجحت 11 تحققًا من الصلاحيات داخل **BEGIN READ ONLY … ROLLBACK** على الإنتاج: لا منحة للحساب الاصطناعي، المستخدم العادي لا يستطيع المنح أو الإلغاء أو القائمة أو قراءة الجدولين الخاصين، anon لا يستطيع قراءة المنحة/القائمة، وservice role لا يستطيع القائمة/المنح بلا أدمن فعّال. استُخدمت هوية اختبار اصطناعية في سياق SQL فقط؛ لا إنشاء مستخدم أو جلسة دخول فعلية.
- الجدولان الجديدان محميان بـRLS وبلا صلاحيات جدول مباشرة لـanon/authenticated/service_role. أغلفة RPC الثلاثة SECURITY INVOKER، والتنفيذ الخاص SECURITY DEFINER بمسار بحث فارغ وEXECUTE محدود. بقي عدد المنح والأوامر صفرًا بعد فحوص النشر.
- طابقت تعريفات الدوال التسع المتوقع حرفيًا: ست دوال لم تتغير؛ ثلاث فقط أضيفت إليها سلطة المنحة/عقود حد المعدل المقصودة. بقيت ACL والملكية للدوال التسع كما قبل النشر. تعريفات الحجز والتسوية والاستهلاك والتجربة لم تُستبدل أو تُخفف.
- بصمات وعدد صفوف السجلات الخمسة المقروءة قبل/بعد متطابقة: `bil_entitlements` (3)، `bil_store_receipts` (0)، `bil_store_entitlement_audit` (10)، `bil_ai_boost_purchases` (0)، `private.bil_admin_ai_boost_grants` (0). حفظت البصمات فقط دون نسخ بيانات الحسابات؛ هذا تحقق لهذه الجداول في تلك اللحظة، لا اختبار شراء على جهاز.
- ملف عداد المدرب الذي طلب المالك عدم تعديله بقي SHA256: `F381A899D8A6352CB7597921950F328A4E40943E9528A54A2ADF0432037CF6FE`.

نتائج النشر، metadata، استعلام الصلاحيات ونتيجته، مقارنة الدوال، البصمات وفحص الأمان محفوظة في `after/`. لم تُجر اختبارات كتابة على حسابات الإنتاج.

## فحص الأمان بعد النشر

لم تظهر تحذيرات WARN أو أخطاء ERROR جديدة. تمت المقارنة مع حذف وقت الرصد فقط:

- [RLS Enabled No Policy](https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy): INFO من 42 إلى 44. الإضافتان هما الجدولان الخاصان الجديدان؛ منع الوصول المباشر مقصود، ومثبت بالصلاحيات واختبار المنع. لا تُضاف سياسة سماح لمجرد إخفاء المعلومة.
- [Signed-In Users Can Execute SECURITY DEFINER](https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable): التحذيرات الـ67 السابقة نفسها، بلا إضافات أو حذف. لا يعني ذلك تدقيق جميع وظائف المشروع ضمن هذا النشر.
- [Leaked Password Protection](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection): التحذير السابق بقي واحدًا؛ لم يُغيّر إعداد Auth خارج النطاق.

## الحدود المتبقية

الخادم منشور؛ ظهور التبويبات والأزرار الجديدة على الهاتف يحتاج بناء نسخة التطبيق من هذا الكود. ما زالت تجربة المالك على نسخة موقعة مطلوبة: جلسة أدمن فعلية، منحة إلى حساب اختبار مسجل، ظهور الرصيد/الميزات عند المستلم، الإلغاء، واستعادة/تجديد شراء فعلي. لم يُدّع نجاح رحلة جهاز أو قبول متجر من اختبارات الخادم.

فرع العمل `codex/bil-unified-release-20260909` بقي على الأساس `8d48db48a9b8b5c59b6c10e8a26868bf590de13f`، مع حفظ التغييرات السابقة دون reset أو تنظيف للـworktree.
