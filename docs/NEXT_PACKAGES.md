# BIL Next Packages

## Active Epic — AI Platform Foundation

`BIL-AI-005` is the current package. It adds deterministic immutable provenance for Truth Engine rule composition while preserving the existing assessment API.

After `BIL-AI-005` passes and is committed, the next package must continue the Truth/Explain foundation from the committed repository state. It must not begin Body Twin, Decision Memory, One Best Action ranking, AI Coach, prompts, provider integration, cloud transport, persistence, or UI unless the living repository documents explicitly authorize that transition.

## Current Gate

`BIL-AI-005` must pass and be committed before the next AI Platform package is issued.

## After BIL-QUALITY-002-R1
Re-run `BIL-AI-005` verification. After it passes and is committed, continue with the first incomplete AI Platform package documented by the AI living roadmap. No production optimization package is authorized unless the stabilized benchmark still fails.
