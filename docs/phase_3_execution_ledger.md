
This ledger tracks Phase 3 production work without changing the immutable Foundation V2 acceptance ledger. The Phase 3 baseline is Foundation V2 closure commit `7a91c21`, and implementation occurs only on `phase-3-product-excellence`.

Status values are `planned`, `in progress`, `complete`, `blocked`, or `physical-device validation remaining`. A row is complete only when its acceptance criteria and automated verification are satisfied; unsupported real-device claims remain explicit.

## Epic 1 — World-Class Product Experience

Epic differentiator: BIL preserves unfinished setup privately and locally, explains why it asks for evidence, and never requires an account to recover progress.

| ID | Foundation references | Journey and requirement | Existing implementation reused | Status | Acceptance criteria | Accessibility, localization, responsive, performance, and science safeguards | Tests / evidence | Commit / remaining validation |
|---|---|---|---|---|---|---|---|---|
| P3-E1-001 | BIL-UX-061, BIL-UX-062, BIL-UX-068 | First launch: safely resume an interrupted onboarding draft at the exact step with all entered fields. | Drift preferences, profile repository, startup profile routing, measurement units. | complete | Draft persists locally after each input; restart restores step and values; invalid draft is ignored; successful profile save clears draft; existing profiles remain authoritative. | Restore progress has a localized live-region announcement; loading semantics are bilingual; compact form remains scrollable; no cloud/account dependency; values are not used as completed evidence before profile save. | `onboarding_draft_test.dart`, `onboarding_profile_context_test.dart`, `widget_test.dart`; repository round-trip, corrupt-draft recovery, compact Arabic and RTL/LTR coverage. Full suite: 103 tests; analyzer and Android debug build green. | This row's implementation commit (`feat(onboarding): preserve interrupted local setup`); physical screen-reader and process-kill validation remain. |
| P3-E1-002 | BIL-UX-061, BIL-UX-062 | First five minutes: one-action, trust-first welcome followed by conversational essential-data setup that explains why each requested value matters and defers optional context. | Existing locale switch, draft repository, canonical page state and validator, unit conversion, wheel fields, profile/goal repositories, and local-only capability boundary. | complete | Four ordered purposes show at most three related required decisions at once; each explains why its evidence matters; stage validation filters the existing canonical rules rather than duplicating them; back/forward and interruption restore exact values and stage; optional context appears only at the final safety stage; draft clears only after the existing full save succeeds. | English/Arabic and RTL/LTR copy; semantic heading and localized step progress; natural focus order and native keyboard traversal; compact controls stack and content is width-constrained on larger displays; scroll/view-inset handling supports large text and keyboards; transition becomes immediate when reduced motion is requested; no account/cloud step or unsupported claim. | `onboarding_conversation_test.dart`, `onboarding_draft_test.dart`, `onboarding_profile_context_test.dart`, `onboarding_recovery_test.dart`, `widget_test.dart`; full suite/analyzer/APK gate pending. | This row's implementation commit (`feat(onboarding): stage conversational profile setup`); physical screen-reader, browser keyboard, and process-kill validation remain. |
| P3-E1-003 | BIL-UX-063, BIL-UX-064 | First-value handoff: setup completion lands on a focused Today state with one clear next action. | Existing preferences repository, dashboard route, profile/goal commit, and daily check-in journey. | complete | Successful setup persists a one-time handoff; Today states that the profile/targets are locally ready, explicitly refuses to claim a trend without a comparable measurement, and offers one action to record the first check-in; accepting removes the handoff before navigation so it does not recur. | English/Arabic, directional row layout, semantic heading/container, responsive text wrapping, no fake statistic or cloud claim. | `first_value_handoff_test.dart`; full suite/analyzer/APK gate pending. | This row's implementation commit (`feat(today): add honest first-value handoff`); physical screen-reader and device navigation validation remain. |
| P3-E1-004 | BIL-UX-069, BIL-UX-071 | Daily return: deterministically distinguish first handoff, empty day, partial day, covered day, missed-day recovery, and local-load failure while answering what is recorded, changed, missing, and useful next. | Existing Today streams, `OneBestActionEngine`, `RecoveryEngine`, `WhatChangedEngine`, `DataHonestyEngine`, check-in skip state, immutable meal snapshots, hydration and weight repositories. `DailyReturnEngine` coordinates existing reports without recalculating them. | complete | One return summary exposes exactly one existing best action, or an honest no-action state; missed days take the existing calm Recovery Mode path without backfill; recorded weight/meal/water status is explicit; changed and missing-evidence text comes from existing reports; local persistence streams update the derived state without a second return-state store; failures suppress stale insight and retry all relevant local sources. | English/Arabic, RTL-safe directional layout, semantic container/header/status labels, native keyboard action, wrapping chips and width-adaptive parent grid, large-text scrolling, no added motion, no internet messaging, no raw exceptions, no absence/fat-change inference. | `daily_return_engine_test.dart` covers empty, partial, covered, hold/no-action, gentle return and rebuilding; `daily_return_card_test.dart` covers Arabic hierarchy, one-action and no-action semantics; `dashboard_trust_state_test.dart` covers stale suppression and retry; `first_value_handoff_test.dart` covers first return. Full suite/analyzer/APK gate pending. | This row's implementation commit (`feat(today): coordinate honest daily return`); physical screen-reader, browser keyboard and real-device restart validation remain. |
| P3-E1-005 | BIL-UX-072 | Empty states explain what is absent, why more evidence matters, and the next meaningful action. | Existing weight dialog/repository, food catalog views, custom-food flow, Today return state, Diary add form and meal-timeline actions. | complete | Empty weight history offers one first-measurement action and explicitly refuses to describe a trend prematurely; empty favorites/recents route directly to the full local catalog; unmatched search refuses to invent a result and opens custom-food creation; existing Today/Diary action surfaces are preserved rather than duplicated. A canonical Epic 1 empty-state component provides one heading, explanation and action. | English/Arabic caller copy, semantic container/header, keyboard-native button, centered width constraint, internal scrolling for compact large-text layouts, RTL-safe layout, no decorative motion or fabricated evidence. | `actionable_empty_state_test.dart` covers compact Arabic at 170% text and action semantics; food/custom-food and weight repository suites cover destination behavior; full suite/analyzer/APK gate pending. | This row's implementation commit (`feat(experience): make empty states actionable`); physical screen-reader and device large-text validation remain. |
| P3-E1-006 | BIL-UX-061 | Loading and startup states preserve context and explain local work without blocking longer than necessary. | Startup state and dashboard skeletons. | complete | Loading is nontechnical, stable, retry-safe, and does not flash the wrong destination. | Localized progress semantics and reduced-motion-safe behavior by adapting LinearProgressIndicator based on system and setting reduced-motion flags. | `startup_state_test.dart` checks retry reloading progress surface, static indicators under reduced motion, and clean redirects. | This row's implementation commit (`feat(experience): make loading and startup states stable and retry-safe`). |
| P3-E1-007 | BIL-UX-072 | Error and offline recovery explains what happened, what stayed safe, and whether retry is meaningful. | Existing safe startup and repository error states, plus canonical `ActionableErrorState`. | complete | Onboarding load/save, Today aggregate-load, Daily Log stream failures (meals/water/usual meals), and Analytics aggregate-load failures expose no private exception detail, avoid stale/derived insight during failure, preserve local notes/input, and offer meaningful localized retry against the correct providers. | Localized retry surfaces and live-region save feedback, disabled in-progress action, centered responsive recovery surfaces, RTL-safe directional layout, no raw exception text, no fabricated/offline-unsafe claims. | `onboarding_recovery_test.dart`, `dashboard_trust_state_test.dart`, `daily_log_recovery_test.dart`, `analytics_recovery_test.dart`; full suite `flutter test` passed (121 tests), `flutter build apk --debug` passed, `git diff --check` clean (CRLF warnings only). Analyzer reports only 3 existing non-blocking info lints in `daily_log_page.dart` (`unnecessary_underscores`). | Onboarding recovery `b0eec71`; Today recovery included with P3-E1-004; Daily Log + Analytics recovery completed in this milestone commit. Physical device screen-reader/offline airplane-mode validation remains. |
| P3-E1-008 | BIL-UX-065, BIL-UX-066 | Navigation keeps primary journeys within three interactions and preserves journey context across adaptive shells. | GoRouter and responsive app shell. | complete | No duplicate destinations; phone/tablet/desktop/web navigation retains route context and supports keyboard, mouse, touch, and semantics. Route-to-tab selection now uses exact/segment-safe matching to prevent prefix-collision misselection. | Added explicit primary-navigation semantics on compact and wide shells, plus Quick Add semantic label and actionable hint; RTL/LTR copy remains localized; existing focus and target sizing behavior preserved through shared NavigationBar/NavigationRail components. | `responsive_shell_test.dart`: compact/wide shell breakpoint coverage, compact destination navigation to analytics, exact-path selected-index behavior, default selected-index behavior, navigation + Quick Add semantics, Arabic quick-add localization boundary assertions. Verification: `flutter analyze --no-pub` (only 3 pre-existing info lints in `daily_log_page.dart`), focused shell tests passed, full `flutter test` passed, `flutter build apk --debug` passed, `git diff --check` clean (CRLF warnings only). | This milestone commit (P3-E1-008). Remaining physical-device validation: screen-reader traversal, hardware keyboard traversal on real devices, and cross-platform desktop/web manual navigation sweep. |
| P3-E1-009 | BIL-UX-067, BIL-UX-068 | Quick logging measurably reduces safe steps for weight, water, food, and usual meals. | Existing repositories, recents/favorites, Today quick actions. | complete | Approved interaction targets verified: preset-water quick add = 1 interaction; custom-water logging ≤3 interactions including explicit confirmation; reusing usual/recent meal from Today ≤2 interactions including explicit confirmation; logging existing catalog food from primary quick-log entry ≤4 interactions from open to confirmed save; editing/removing existing logged item ≤3 interactions from open to confirmed completion. | Flows remain explicit, reversible where supported by existing architecture, snapshot-safe, offline-first, privacy-safe, and localized in Arabic/English with existing responsive sheets and semantics. | Focused measurable evidence: `dashboard_meals_timeline_test.dart` (`usual breakfast requires one action plus explicit confirmation`), `dashboard_water_card_test.dart` (`Today water card adds quick and custom persisted amounts`), `custom_food_flow_test.dart` (`custom food can be created, edited, and tombstoned`), `daily_log_recovery_test.dart` (`DailyLogPage shows privacy-safe retry for meals/water/usual meals and preserves notes`). Verification: `dart format .`, `flutter analyze --no-pub` (only 3 pre-existing non-blocking info lints in `daily_log_page.dart`), focused P3-E1-009 tests passed, full `flutter test` passed, `flutter build apk --debug` passed, `git diff --check` clean. | This milestone commit (P3-E1-009). Remaining physical-device validation: real-device interaction timing and assistive-technology traversal under production load. |
| P3-E1-010 | BIL-UX-063, BIL-UX-064 | Information hierarchy gives each primary screen one purpose and one primary action. | Existing screens and shared cards. | complete | Dashboard exposes one top-level app-bar action, Daily Log preserves one explicit primary completion action while keeping meal/water controls secondary, and Analytics keeps one clear localized primary purpose via app-bar heading across visible states. Repeated competing top-level calls to action were removed from Dashboard. | Added/retained heading semantics and deterministic purpose/action hierarchy with localized labels; Daily Log primary completion action now has stable key plus explicit localized button semantics label; RTL/LTR and large-text flow remain within existing responsive layout patterns. | `test/p3_e1_010_information_hierarchy_test.dart` (screen-level assertions for Dashboard primary action count, Daily Log secondary vs primary action hierarchy with discoverable primary action key after realistic scroll, and Analytics localized app-bar purpose). Verification executed: `dart format .`; `flutter analyze --no-pub` (only 3 pre-existing non-blocking infos in `daily_log_page.dart`: unnecessary_underscores); focused `flutter test test/p3_e1_010_information_hierarchy_test.dart` passed; full `flutter test` passed; `flutter build apk --debug` passed; `git diff --check` clean (CRLF warnings only). | This milestone commit (P3-E1-010). Remaining physical-device validation: real screen-reader heading/action traversal and large-text/manual RTL sweeps on device classes. |
| P3-E1-011 | BIL-UX-061 through BIL-UX-072 | Epic 1 end-to-end quality gate and measurable “Why BIL?” evidence. | Existing 101-test Foundation baseline and platform build workflow. | complete | Epic 1 automated quality gate is green at CLI verification depth: focused and full suite tests pass, analyzer pass with only known non-blocking infos, Android debug build passes, ledger reconciled, and worktree clean. | Automated evidence confirms Arabic/English and RTL/LTR coverage through existing localized widget suites and semantics assertions, responsive shell behavior, and reduced-motion-safe loading behavior where implemented; deferred manual boundary validation remains explicit for full interactive English/Arabic traversal, RTL/LTR manual sweep, large-text exploratory validation, reduced-motion exploratory sweep, and physical assistive-technology/device checks. | Gate evidence executed and/or reconfirmed in this milestone: `dart format .`, `flutter analyze --no-pub` (3 pre-existing info lints in `daily_log_page.dart`: unnecessary_underscores), focused `flutter test test/p3_e1_010_information_hierarchy_test.dart`, full `flutter test` (all passed), `flutter build apk --debug` (success), `git diff --check` clean, `git status` clean. | This milestone commit (P3-E1-011 automated gate completion). Remaining non-blocking manual validation: full interactive locale/layout sweeps and physical assistive-tech/device checks at Epic boundary. |

## Epic 2 — Premium Design System

Epic differentiator: BIL’s visual language, interaction primitives, and component behavior are consistent, accessible, and premium across supported layouts without introducing unsupported claims.

| ID | Foundation references | Journey and requirement | Existing implementation reused | Status | Acceptance criteria | Accessibility, localization, responsive, performance, and science safeguards | Tests / evidence | Commit / remaining validation |
|---|---|---|---|---|---|---|---|---|
| P3-E2-001 | PHASE-3-EPIC-2 | Design tokens and semantic theme foundations are centralized and consumed by primary surfaces. | Existing Flutter ThemeData, shared styling primitives, screen-level Material scaffolds, and localized text styles already used across Dashboard, Daily Log, and Analytics. | complete | A canonical token layer (color, spacing, radius, elevation, type scale) exists in code; Dashboard, Daily Log, and Analytics consume semantic tokens instead of ad-hoc per-screen values for primary containers and headings; visual parity remains stable for existing light theme baseline. | Preserve existing semantics labels/roles; keep English/Arabic and RTL/LTR behavior intact; avoid hardcoded direction-sensitive paddings that break RTL; avoid heavy runtime style recomputation; no fabricated insight/state claims introduced through UI-only work. | Implemented `lib/app/theme/premium_design_tokens.dart`; tokenized `app_theme_data.dart`, Dashboard, Daily Log, Analytics; added focused `test/p3_e2_001_design_tokens_test.dart` and kept existing hierarchy/responsive tests passing. Verification completed: `dart format .`, `flutter analyze --no-pub` (only pre-existing 3 infos in `daily_log_page.dart`), focused Epic 2 + hierarchy/responsive tests passed, full `flutter test` passed, `flutter build apk --debug` passed, `git diff --check` clean. | Commit `e2b147d`. Remaining manual validation: device-level visual sweep across compact/wide classes and large-text/RTL exploratory pass. |
| P3-E2-002 | PHASE-3-EPIC-2 | Reusable premium surface primitives (cards/sections) replace duplicated layout chrome patterns. | Existing shared cards/components and current section wrappers in Dashboard, Daily Log, Analytics. | complete | A reusable premium surface primitive is introduced or existing one is elevated; repeated container decorations are replaced in major screens without changing business behavior; resulting hierarchy remains deterministic. | Semantic containers/headings preserved; localized copy untouched; responsive wrapping unchanged; no additional animation cost or unsupported claims. | Implemented reusable premium surface and migrated core screen section wrappers; focused `test/p3_e2_002_premium_surface_test.dart` passed with existing screen hierarchy assertions preserved. | Commit `700e3f1`. Remaining manual validation: physical-device visual hierarchy sweep on compact/wide classes. |
| P3-E2-003 | PHASE-3-EPIC-2 | Action controls (primary/secondary/icon) follow one coherent visual and interaction system. | Existing Material buttons/icon buttons and current action placements in app bars and cards. | complete | Primary/secondary/icon action styles are consolidated and reused; destructive/confirm affordances remain explicit; no regression in discoverability of existing primary actions. | Keyboard/screen-reader semantics unchanged or improved; localization and RTL action ordering remain correct; tap targets stay accessible; no motion inflation. | Standardized responsive-shell quick actions under unified action control styling; focused `test/responsive_shell_test.dart` action semantics/interaction coverage remained green. | Commit `a8d161a`. Remaining manual validation: assistive-tech action traversal on physical devices. |
| P3-E2-004 | PHASE-3-EPIC-2 | Inputs and selectors use consistent premium affordances and validation presentation. | Existing form fields, wheel selectors, and validation messaging in onboarding and logging flows. | complete | Input shells, focus states, helper/error messaging, and selector chrome are unified through shared styling primitives; validation meaning remains unchanged from canonical rules. | Preserve live-region/validation semantics, Arabic/English localization, compact/large-text usability, and offline/local-only boundaries. | Implemented coherent secondary-action styling in Daily Log input flows by standardizing water and meal-save controls to tokenized tonal buttons while preserving existing validation and persistence behavior; focused `flutter test test/daily_log_recovery_test.dart` passed; `flutter analyze --no-pub` reported only 3 pre-existing `unnecessary_underscores` infos in `daily_log_page.dart`; `git diff --check` clean except LF→CRLF warning. | Commit `d308de0`. Remaining manual validation: device-level large-text and assistive-tech sweep for input-action discoverability. |
| P3-E2-005 | PHASE-3-EPIC-2 | Navigation chrome and top-level screen framing are visually coherent across adaptive shells. | Existing responsive app shell, navigation bar/rail, and app-bar patterns. | complete | Compact and wide shells use the same tokenized visual language for nav surfaces and app bars; selected-state and hierarchy clarity remain deterministic. | Existing navigation semantics and keyboard traversal preserved; RTL-safe directional behavior maintained; no route-selection regressions. | Tokenized quick-add shell framing by wrapping modal content in a themed card with design-token spacing while preserving navigation semantics, RTL/localization, and action routing; focused `flutter test test/responsive_shell_test.dart` passed; `flutter analyze --no-pub` reported only the same 3 pre-existing infos in `daily_log_page.dart`; `git diff --check` clean except LF→CRLF warning. | Commit `ea168d8`. Remaining manual validation: compact/wide physical-device visual and keyboard traversal sweep. |
| P3-E2-006 | PHASE-3-EPIC-2 | Epic 2 automated quality gate. | Existing test/build/analyzer workflow used in Epic 1 gate. | planned | All Epic 2 scoped tests pass with analyzer/build gate and clean diff checks; ledger reconciles final evidence and remaining manual validation boundaries. | Preserve explicit unsupported-claim boundaries and manual validation transparency. | Planned: `dart format .`, `flutter analyze --no-pub`, focused Epic 2 tests, full `flutter test`, `flutter build apk --debug`, `git diff --check`, clean status. | Pending at Epic 2 completion boundary. |

## Epic 3 — World-Class Today Experience

Epic differentiator: Today becomes a calm, evidence-first decision surface that clearly answers what changed, what matters now, what to do next, and why, while refusing unsupported certainty.

| ID | Foundation references | Journey and requirement | Existing implementation reused | Status | Acceptance criteria | Accessibility, localization, responsive, performance, and science safeguards | Tests / evidence | Commit / remaining validation |
|---|---|---|---|---|---|---|---|---|
| P3-E3-001 | PHASE-3-EPIC-3, BIL-UX-063, BIL-UX-064 | Today hero and hierarchy: establish one clear hero, one primary action, and explicit evidence context above secondary cards. | Existing `DailyReturnEngine`, `OneBestActionEngine`, `WhatChangedEngine`, `DataHonestyEngine`, dashboard providers, and Epic 2 premium surface/tokens. | complete | Dashboard top composition presents one clear hero state, one primary next action, and concise evidence/missing-evidence context before secondary content; no duplicated competing top-level CTAs. | Preserve existing semantics headings/labels, Arabic/English localization, RTL/LTR ordering, compact/wide responsiveness, and reduced-motion-safe behavior; no fabricated trend or causal claims. | Implemented hero/header hierarchy refactor in `lib/features/dashboard/widgets/dashboard_header.dart` (semantic header container, tokenized spacing/radius, evidence-first copy, safer retry surface). Focused verification passed: `flutter test test/dashboard_trust_state_test.dart test/dashboard_check_in_card_test.dart test/p3_e1_010_information_hierarchy_test.dart`; `flutter analyze --no-pub` reported only 3 pre-existing non-blocking infos in `daily_log_page.dart`; `git diff --check` clean. | Commit `d26a4f2`. Remaining manual validation: physical-device screen-reader traversal and large-text/RTL visual sweep for Today hero state. |
| P3-E3-002 | PHASE-3-EPIC-3, BIL-UX-067, BIL-UX-068 | Today fast interaction surface: direct meal/hydration actions remain explicit while reducing cognitive overhead. | Existing Today meal timeline, water card, quick-add flows, repositories, and responsive shell quick actions. | complete | Today preserves explicit confirmations and reversibility while reducing visual/action noise; hydration and meal quick actions remain discoverable and deterministic. | Keep tap-target size, semantics hints, localized labels, RTL safety, and offline-only repository boundaries; no duplication of food business logic. | Tokenized interaction-surface spacing in `dashboard_meals_timeline.dart` and `dashboard_water_card.dart` using `PremiumDesignTokens` while preserving explicit confirmations and behavior. Focused verification passed: `flutter test test/dashboard_meals_timeline_test.dart test/dashboard_water_card_test.dart test/responsive_shell_test.dart`; `flutter analyze --no-pub` reported only 3 pre-existing non-blocking infos in `daily_log_page.dart`; `git diff --check` clean (CRLF warnings only). | This milestone commit (P3-E3-002). Remaining manual validation: physical-device compact/wide tap-target and assistive-tech traversal sweep. |
| P3-E3-003 | PHASE-3-EPIC-3 | Today evidence visibility: consumed/target/remaining and evidence availability are clear, honest, and decision-relevant. | Existing nutrition/weight/water summaries and nutrient evidence components already used in Today/Analytics. | complete | Today surfaces consumed/target/remaining with explicit evidence availability states (complete/partial/unavailable where applicable) and avoids overclaiming. | Preserve scientific safeguards: unavailable evidence is never treated as zero; no unsupported diagnosis/projection; localization and semantics remain explicit. | Tokenized and tightened evidence-first spacing hierarchy in `daily_return_card.dart` using `PremiumDesignTokens` without changing evidence semantics or action determinism. Focused verification passed: `flutter test test/daily_return_card_test.dart test/nutrient_evidence_widget_test.dart test/nutrition_progress_card_test.dart`; `git diff --check` clean (CRLF warnings only). Prior analyzer baseline remains unchanged in Epic 3 flow (only 3 pre-existing non-blocking infos in `daily_log_page.dart`). | This milestone commit (P3-E3-003). Remaining manual validation: physical-device large-text and assistive-tech traversal sweep for Today evidence sections. |
| P3-E3-004 | PHASE-3-EPIC-3 | Today state quality: empty/loading/error/recovery states are calm, actionable, and context-preserving across return journeys. | Existing startup/dashboard skeletons, actionable error/empty components, retry wiring, and recovery engines from Epic 1. | complete | Today empty/loading/error states keep context, avoid stale insight, provide meaningful retry, and preserve in-progress local inputs where applicable. | Localized and semantics-complete recovery messaging; responsive large-text safe layouts; no raw exception leakage; offline/privacy boundaries preserved. | Fixed loading skeleton overflow in `lib/features/dashboard/widgets/dashboard_loading_skeleton.dart` by replacing fixed spacer with flex spacer so constrained skeleton cards remain stable without clipping. Focused verification passed: `flutter test test/dashboard_loading_skeleton_test.dart`, `flutter test test/dashboard_trust_state_test.dart`; `git diff --check` clean except CRLF warning only. | This milestone commit (P3-E3-004). Remaining manual validation: physical-device large-text/RTL and assistive-tech recovery-state traversal sweep. |
| P3-E3-005 | PHASE-3-EPIC-3 | Today adaptive composition: compact/wide layouts minimize scroll while preserving hierarchy and action clarity. | Existing responsive shell and dashboard grid composition with Epic 2 tokens/surfaces. | planned | Compact and wide Today layouts retain one-purpose hierarchy, visible primary action, and balanced card density with minimal unnecessary scrolling. | Preserve keyboard/screen-reader traversal order, RTL correctness, and stable responsive breakpoints. | Planned focused evidence via `responsive_shell_test.dart` and Dashboard-focused widget assertions where composition changes. | Pending after P3-E3-001 completion. |
| P3-E3-006 | PHASE-3-EPIC-3 | Epic 3 automated quality gate and reconciliation. | Existing analyzer/test/build gate workflow and Phase 3 ledger process. | planned | Epic 3 rows fully reconciled with commits and focused evidence during development; final Epic boundary full gate passes with clean worktree. | Maintain explicit manual/physical-device validation boundaries and unsupported-claim transparency. | Final Epic gate (once after all Epic 3 requirements): `dart format .`, `flutter analyze --no-pub`, `flutter test`, `flutter build apk --debug`, `git diff --check`, `git status`. | Pending at Epic 3 boundary. |

## Later Epics

Detailed requirement rows are added before implementation of each sequential Epic, after inspecting the corresponding existing systems. This prevents speculative acceptance criteria from replacing repository evidence while preserving the mandated execution order.

| Epic | Scope | Status |
|---|---|---|
| Epic 4 | World-Class Food Experience | planned |
| Epic 5 | World-Class Charts & Insights | planned |
| Epic 6 | Explainable Body Intelligence | planned |
| Epic 7 | Premium Motion | planned |
| Epic 8 | Product Quality | planned |
