This ledger tracks Phase 3 production work without changing the immutable Foundation V2 acceptance ledger. The Phase 3 baseline is Foundation V2 closure commit `7a91c21`, and implementation occurs only on `phase-3-product-excellence`.

Status values are `planned`, `in progress`, `complete`, `blocked`, or `physical-device validation remaining`. A row is complete only when its acceptance criteria and automated verification are satisfied; unsupported real-device claims remain explicit.

## Epic 1 — World-Class Product Experience

Epic differentiator: BIL preserves unfinished setup privately and locally, explains why it asks for evidence, and never requires an account to recover progress.

| ID | Foundation references | Journey and requirement | Existing implementation reused | Status | Acceptance criteria | Accessibility, localization, responsive, performance, and science safeguards | Tests / evidence | Commit / remaining validation |
|---|---|---|---|---|---|---|---|---|
| P3-E1-001 | BIL-UX-061, BIL-UX-062, BIL-UX-068 | First launch: safely resume an interrupted onboarding draft at the exact step with all entered fields. | Drift preferences, profile repository, startup profile routing, measurement units. | complete | Draft persists locally after each input; restart restores step and values; invalid draft is ignored; successful profile save clears draft; existing profiles remain authoritative. | Restore progress has a localized live-region announcement; loading semantics are bilingual; compact form remains scrollable; no cloud/account dependency; values are not used as completed evidence before profile save. | `onboarding_draft_test.dart`, `onboarding_profile_context_test.dart`, `widget_test.dart`; repository round-trip, corrupt-draft recovery, compact Arabic and RTL/LTR coverage. Full suite: 103 tests; analyzer and Android debug build green. | This row's implementation commit (`feat(onboarding): preserve interrupted local setup`); physical screen-reader and process-kill validation remain. |
| P3-E1-002 | BIL-UX-061, BIL-UX-062 | First five minutes: one-action, trust-first welcome followed by conversational essential-data setup that explains why each requested value matters and defers optional context. | Existing locale switch, draft repository, canonical page state and validator, unit conversion, wheel fields, profile/goal repositories, and local-only capability boundary. | complete | Four ordered purposes show at most three related required decisions at once; each explains why its evidence matters; stage validation filters the existing canonical rules rather than duplicating them; back/forward and interruption restore exact values and stage; optional context appears only at the final safety stage; draft clears only after the existing full save succeeds. | English/Arabic and RTL/LTR copy; semantic heading and localized step progress; natural focus order and native keyboard traversal; compact controls stack and content is width-constrained on larger displays; scroll/view-inset handling supports large text and keyboards; transition becomes immediate when reduced motion is requested; no account/cloud step or unsupported claim. | `onboarding_conversation_test.dart`, `onboarding_draft_test.dart`, `onboarding_profile_context_test.dart`, `onboarding_recovery_test.dart`, `widget_test.dart`; full suite/analyzer/APK gate pending. | This row's implementation commit (`feat(onboarding): stage conversational profile setup`); physical screen-reader, browser keyboard, and process-kill validation remain. |
| P3-E1-003 | BIL-UX-063, BIL-UX-064 | First-value handoff: setup completion lands on a focused Today state with one clear next action. | Existing preferences repository, dashboard route, profile/goal commit, and daily check-in journey. | complete | Successful setup persists a one-time handoff; Today states that the profile/targets are locally ready, explicitly refuses to claim a trend without a comparable measurement, and offers one action to record the first check-in; accepting removes the handoff before navigation so it does not recur. | English/Arabic, directional row layout, semantic heading/container, responsive text wrapping, no fake statistic or cloud claim. | `first_value_handoff_test.dart`; full suite/analyzer/APK gate pending. | This row's implementation commit (`feat(today): add honest first-value handoff`); physical screen-reader and device navigation validation remain. |
| P3-E1-004 | BIL-UX-069, BIL-UX-071 | Daily return: deterministically distinguish first handoff, empty day, partial day, covered day, missed-day recovery, and local-load failure while answering what is recorded, changed, missing, and useful next. | Existing Today streams, `OneBestActionEngine`, `RecoveryEngine`, `WhatChangedEngine`, `DataHonestyEngine`, check-in skip state, immutable meal snapshots, hydration and weight repositories. `DailyReturnEngine` coordinates existing reports without recalculating them. | complete | One return summary exposes exactly one existing best action, or an honest no-action state; missed days take the existing calm Recovery Mode path without backfill; recorded weight/meal/water status is explicit; changed and missing-evidence text comes from existing reports; local persistence streams update the derived state without a second return-state store; failures suppress stale insight and retry all relevant local sources. | English/Arabic, RTL-safe directional layout, semantic container/header/status labels, native keyboard action, wrapping chips and width-adaptive parent grid, large-text scrolling, no added motion, no internet messaging, no raw exceptions, no absence/fat-change inference. | `daily_return_engine_test.dart` covers empty, partial, covered, hold/no-action, gentle return and rebuilding; `daily_return_card_test.dart` covers Arabic hierarchy, one-action and no-action semantics; `dashboard_trust_state_test.dart` covers stale suppression and retry; `first_value_handoff_test.dart` covers first return. Full suite/analyzer/APK gate pending. | This row's implementation commit (`feat(today): coordinate honest daily return`); physical screen-reader, browser keyboard and real-device restart validation remain. |
| P3-E1-005 | BIL-UX-072 | Empty states explain what is absent, why more evidence matters, and the next meaningful action. | Existing weight dialog/repository, food catalog views, custom-food flow, Today return state, Diary add form and meal-timeline actions. | complete | Empty weight history offers one first-measurement action and explicitly refuses to describe a trend prematurely; empty favorites/recents route directly to the full local catalog; unmatched search refuses to invent a result and opens custom-food creation; existing Today/Diary action surfaces are preserved rather than duplicated. A canonical Epic 1 empty-state component provides one heading, explanation and action. | English/Arabic caller copy, semantic container/header, keyboard-native button, centered width constraint, internal scrolling for compact large-text layouts, RTL-safe layout, no decorative motion or fabricated evidence. | `actionable_empty_state_test.dart` covers compact Arabic at 170% text and action semantics; food/custom-food and weight repository suites cover destination behavior; full suite/analyzer/APK gate pending. | This row's implementation commit (`feat(experience): make empty states actionable`); physical screen-reader and device large-text validation remain. |
| P3-E1-006 | BIL-UX-061 | Loading and startup states preserve context and explain local work without blocking longer than necessary. | Startup state and dashboard skeletons. | complete | Loading is nontechnical, stable, retry-safe, and does not flash the wrong destination. | Localized progress semantics and reduced-motion-safe behavior by adapting LinearProgressIndicator based on system and setting reduced-motion flags. | `startup_state_test.dart` checks retry reloading progress surface, static indicators under reduced motion, and clean redirects. | This row's implementation commit (`feat(experience): make loading and startup states stable and retry-safe`). |
| P3-E1-007 | BIL-UX-072 | Error and offline recovery explains what happened, what stayed safe, and whether retry is meaningful. | Existing safe startup and repository error states, plus canonical `ActionableErrorState`. | complete | Onboarding load/save, Today aggregate-load, Daily Log stream failures (meals/water/usual meals), and Analytics aggregate-load failures expose no private exception detail, avoid stale/derived insight during failure, preserve local notes/input, and offer meaningful localized retry against the correct providers. | Localized retry surfaces and live-region save feedback, disabled in-progress action, centered responsive recovery surfaces, RTL-safe directional layout, no raw exception text, no fabricated/offline-unsafe claims. | `onboarding_recovery_test.dart`, `dashboard_trust_state_test.dart`, `daily_log_recovery_test.dart`, `analytics_recovery_test.dart`; full suite `flutter test` passed (121 tests), `flutter build apk --debug` passed, `git diff --check` clean (CRLF warnings only). Analyzer reports only 3 existing non-blocking info lints in `daily_log_page.dart` (`unnecessary_underscores`). | Onboarding recovery `b0eec71`; Today recovery included with P3-E1-004; Daily Log + Analytics recovery completed in this milestone commit. Physical device screen-reader/offline airplane-mode validation remains. |
| P3-E1-008 | BIL-UX-065, BIL-UX-066 | Navigation keeps primary journeys within three interactions and preserves journey context across adaptive shells. | GoRouter and responsive app shell. | complete | No duplicate destinations; phone/tablet/desktop/web navigation retains route context and supports keyboard, mouse, touch, and semantics. Route-to-tab selection now uses exact/segment-safe matching to prevent prefix-collision misselection. | Added explicit primary-navigation semantics on compact and wide shells, plus Quick Add semantic label and actionable hint; RTL/LTR copy remains localized; existing focus and target sizing behavior preserved through shared NavigationBar/NavigationRail components. | `responsive_shell_test.dart`: compact/wide shell breakpoint coverage, compact destination navigation to analytics, exact-path selected-index behavior, default selected-index behavior, navigation + Quick Add semantics, Arabic quick-add localization boundary assertions. Verification: `flutter analyze --no-pub` (only 3 pre-existing info lints in `daily_log_page.dart`), focused shell tests passed, full `flutter test` passed, `flutter build apk --debug` passed, `git diff --check` clean (CRLF warnings only). | This milestone commit (P3-E1-008). Remaining physical-device validation: screen-reader traversal, hardware keyboard traversal on real devices, and cross-platform desktop/web manual navigation sweep. |
| P3-E1-009 | BIL-UX-067, BIL-UX-068 | Quick logging measurably reduces safe steps for weight, water, food, and usual meals. | Existing repositories, recents/favorites, Today quick actions. | complete | Approved interaction targets verified: preset-water quick add = 1 interaction; custom-water logging ≤3 interactions including explicit confirmation; reusing usual/recent meal from Today ≤2 interactions including explicit confirmation; logging existing catalog food from primary quick-log entry ≤4 interactions from open to confirmed save; editing/removing existing logged item ≤3 interactions from open to confirmed completion. | Flows remain explicit, reversible where supported by existing architecture, snapshot-safe, offline-first, privacy-safe, and localized in Arabic/English with existing responsive sheets and semantics. | Focused measurable evidence: `dashboard_meals_timeline_test.dart` (`usual breakfast requires one action plus explicit confirmation`), `dashboard_water_card_test.dart` (`Today water card adds quick and custom persisted amounts`), `custom_food_flow_test.dart` (`custom food can be created, edited, and tombstoned`), `daily_log_recovery_test.dart` (`DailyLogPage shows privacy-safe retry for meals/water/usual meals and preserves notes`). Verification: `dart format .`, `flutter analyze --no-pub` (only 3 pre-existing non-blocking info lints in `daily_log_page.dart`), focused P3-E1-009 tests passed, full `flutter test` passed, `flutter build apk --debug` passed, `git diff --check` clean. | This milestone commit (P3-E1-009). Remaining physical-device validation: real-device interaction timing and assistive-technology traversal under production load. |
| P3-E1-010 | BIL-UX-063, BIL-UX-064 | Information hierarchy gives each primary screen one purpose and one primary action. | Existing screens and shared cards. | complete | Dashboard exposes one top-level app-bar action, Daily Log preserves one explicit primary completion action while keeping meal/water controls secondary, and Analytics keeps one clear localized primary purpose via app-bar heading across visible states. Repeated competing top-level calls to action were removed from Dashboard. | Added/retained heading semantics and deterministic purpose/action hierarchy with localized labels; Daily Log primary completion action now has stable key plus explicit localized button semantics label; RTL/LTR and large-text flow remain within existing responsive layout patterns. | `test/p3_e1_010_information_hierarchy_test.dart` (screen-level assertions for Dashboard primary action count, Daily Log secondary vs primary action hierarchy with discoverable primary action key after realistic scroll, and Analytics localized app-bar purpose). Verification executed: `dart format .`; `flutter analyze --no-pub` (only 3 pre-existing non-blocking infos in `daily_log_page.dart`: unnecessary_underscores); focused `flutter test test/p3_e1_010_information_hierarchy_test.dart` passed; full `flutter test` passed; `flutter build apk --debug` passed; `git diff --check` clean (CRLF warnings only). | This milestone commit (P3-E1-010). Remaining physical-device validation: real screen-reader heading/action traversal and large-text/manual RTL sweeps on device classes. |
| P3-E1-011 | BIL-UX-061 through BIL-UX-072 | Epic 1 end-to-end quality gate and measurable “Why BIL?” evidence. | Existing 101-test Foundation baseline and platform build workflow. | complete | Epic 1 automated quality gate is green at CLI verification depth: focused and full suite tests pass, analyzer pass with only known non-blocking infos, Android debug build passes, ledger reconciled, and worktree clean. | Automated evidence confirms Arabic/English and RTL/LTR coverage through existing localized widget suites and semantics assertions, responsive shell behavior, and reduced-motion-safe loading behavior where implemented; deferred manual boundary validation remains explicit for full interactive English/Arabic traversal, RTL/LTR manual sweep, large-text exploratory validation, reduced-motion exploratory sweep, and physical assistive-technology/device checks. | Gate evidence executed and/or reconfirmed in this milestone: `dart format .`, `flutter analyze --no-pub` (3 pre-existing info lints in `daily_log_page.dart`: unnecessary_underscores), focused `flutter test test/p3_e1_010_information_hierarchy_test.dart`, full `flutter test` (all passed), `flutter build apk --debug` (success), `git diff --check` clean, `git status` clean. | This milestone commit (P3-E1-011 automated gate completion). Remaining non-blocking manual validation: full interactive locale/layout sweeps and physical assistive-tech/device checks at Epic boundary. |

## Epic 2 — Premium Design System

Epic differentiator: BIL’s visual language, interaction primitives, and component behavior are consistent, accessible, and premium across supported layouts without introducing unsupported claims.

| ID | Foundation references | Journey and requirement | Existing implementation reused | Status | Acceptance criteria | Accessibility, localization, responsive, performance, and science safeguards | Tests / evidence | Commit / remaining validation |
|---|---|---|---|---|---|---|---|---|
| P3-E2-001 | PHASE-3-EPIC-2 | Design tokens and semantic theme foundations are centralized and consumed by primary surfaces. | Existing Flutter ThemeData, shared styling primitives, screen-level Material scaffolds, and localized text styles already used across Dashboard, Daily Log, and Analytics. | complete | A canonical token layer (color, spacing, radius, elevation, type scale) exists in code; Dashboard, Daily Log, and Analytics consume semantic tokens instead of ad-hoc per-screen values for primary containers and headings; visual parity remains stable for existing light theme baseline. | Preserve existing semantics labels/roles; keep English/Arabic and RTL/LTR behavior intact; avoid hardcoded direction-sensitive paddings that break RTL; avoid heavy runtime style recomputation; no fabricated insight/state claims introduced through UI-only work. | Implemented `lib/app/theme/premium_design_tokens.dart`; tokenized `app_theme_data.dart`, Dashboard, Daily Log, Analytics; added focused `test/p3_e2_001_design_tokens_test.dart` and kept existing hierarchy/responsive tests passing. Verification completed: `dart format .`, `flutter analyze --no-pub` (only pre-existing 3 infos in `daily_log_page.dart`), focused Epic 2 + hierarchy/responsive tests passed, full `flutter test` passed, `flutter build apk --debug` passed, `git diff --check` clean. | Commit `e2b147d`. Remaining manual validation: device-level visual sweep across compact/wide classes and large-text/RTL exploratory pass. |
| P3-E2-002 | PHASE-3-EPIC-2 | Reusable premium surface primitives (cards/sections) replace duplicated layout chrome patterns. | Existing shared cards/components and current section wrappers in Dashboard, Daily Log, Analytics. | complete | A reusable premium surface primitive is introduced or existing one is elevated; repeated container decorations are replaced in major screens without changing business behavior; resulting hierarchy remains deterministic. | Semantic containers/headings preserved; localized copy untouched; responsive wrapping unchanged; no additional animation cost or unsupported claims. | Implemented reusable premium surface and migrated core screen section wrappers; focused `test/p3_e2_002_premium_surface_test.dart` passed with existing screen hierarchy assertions preserved. | Commit `700e3f1`. Remaining manual validation: physical-device visual hierarchy sweep on compact/wide classes. |
| P3-E2-003 | PHASE-3-EPIC-2 | Action controls (primary/secondary/icon) follow one coherent visual and interaction system. | Existing Material buttons/icon buttons and current action placements in app bars and cards. | complete | Primary/secondary/icon action styles are consolidated and reused; destructive/confirm affordances remain explicit; no regression in discoverability of existing primary actions. | Keyboard/screen-reader semantics unchanged or improved; localization and RTL action ordering remain correct; tap targets stay accessible; no motion inflation. | Standardized responsive-shell quick actions under unified action control styling; focused `test/responsive_shell_test.dart` action semantics/interaction coverage remained green. | Commit `a8d161a`. Remaining manual validation: assistive-tech action traversal on physical devices. |
| P3-E2-004 | PHASE-3-EPIC-2 | Inputs and selectors use consistent premium affordances and validation presentation. | Existing form fields, wheel selectors, and validation messaging in onboarding and logging flows. | complete | Input shells, focus states, helper/error messaging, and selector chrome are unified through shared styling primitives; validation meaning remains unchanged from canonical rules. | Preserve live-region/validation semantics, Arabic/English localization, compact/large-text usability, and offline/local-only boundaries. | Implemented coherent secondary-action styling in Daily Log input flows by standardizing water and meal-save controls to tokenized tonal buttons while preserving existing validation and persistence behavior; focused `flutter test test/daily_log_recovery_test.dart` passed; `flutter analyze --no-pub` reported only 3 pre-existing `unnecessary_underscores` infos in `daily_log_page.dart`; `git diff --check` clean except LF→CRLF warning. | Commit `d308de0`. Remaining manual validation: device-level large-text and assistive-tech sweep for input-action discoverability. |
| P3-E2-005 | PHASE-3-EPIC-2 | Navigation chrome and top-level screen framing are visually coherent across adaptive shells. | Existing responsive app shell, navigation bar/rail, and app-bar patterns. | complete | Compact and wide shells use the same tokenized visual language for nav surfaces and app bars; selected-state and hierarchy clarity remain deterministic. | Existing navigation semantics and keyboard traversal preserved; RTL-safe directional behavior maintained; no route-selection regressions. | Tokenized quick-add shell framing by wrapping modal content in a themed card with design-token spacing while preserving navigation semantics, RTL/localization, and action routing; focused `flutter test test/responsive_shell_test.dart` passed; `flutter analyze --no-pub` reported only the same 3 pre-existing infos in `daily_log_page.dart`; `git diff --check` clean except LF→CRLF warning. | Commit `ea168d8`. Remaining manual validation: compact/wide physical-device visual and keyboard traversal sweep. |
| P3-E2-006 | PHASE-3-EPIC-2 | Epic 2 automated quality gate. | Existing test/build/analyzer workflow used in Epic 1 gate. | planned | All Epic 2 scoped tests pass with analyzer/build gate and clean diff checks; ledger reconciles final evidence and remaining manual validation boundaries. | Preserve explicit unsupported-claim boundaries and manual validation transparency. | Planned: `dart format .`, `flutter analyze --no-pub`, focused Epic 2 tests, full `flutter test`, `flutter build apk --debug`, `git diff --check`, clean status. | Pending at Epic 2 completion boundary. |

## Epic 3 — World-Class Today Experience

Epic differentiator: Today becomes a calm, evidence-first decision surface that clearly answers what changed, what matters now, what to do next, and why, while refusing unsupported certainty.

| ID | Foundation references | Journey and requirement | Existing implementation reused | Status | Acceptance criteria | Accessibility, localization, responsive, performance, and science safeguards | Tests / evidence | Commit / remaining validation |
|---|---|---|---|---|---|---|---|---|
| P3-E3-001 | PHASE-3-EPIC-3, BIL-UX-063, BIL-UX-064 | Today hero and hierarchy: establish one clear hero, one primary action, and explicit evidence context above secondary cards. | Existing `DailyReturnEngine`, `OneBestActionEngine`, `WhatChangedEngine`, `DataHonestyEngine`, dashboard providers, and Epic 2 premium surface/tokens. | complete | Dashboard top composition presents one clear hero state, one primary next action, and concise evidence/missing-evidence context before secondary content; no duplicated competing top-level CTAs. | Preserve existing semantics headings/labels, Arabic/English localization, RTL/LTR ordering, compact/wide responsiveness, and reduced-motion-safe behavior; no fabricated trend or causal claims. | Implemented hero/header hierarchy refactor in `lib/features/dashboard/widgets/dashboard_header.dart` (semantic header container, tokenized spacing/radius, evidence-first copy, safer retry surface). Focused verification passed: `flutter test test/dashboard_trust_state_test.dart test/dashboard_check_in_card_test.dart test/p3_e1_010_information_hierarchy_test.dart`; `flutter analyze --no-pub` reported only 3 pre-existing non-blocking infos in `daily_log_page.dart`; `git diff --check` clean. | Commit `d26a4f2`. Remaining manual validation: physical-device screen-reader traversal and large-text/RTL visual sweep for Today hero state. |
| P3-E3-002 | PHASE-3-EPIC-3, BIL-UX-067, BIL-UX-068 | Today fast interaction surface: direct meal/hydration actions remain explicit while reducing cognitive overhead. | Existing Today meal timeline, water card, quick-add flows, repositories, and responsive shell quick actions. | complete | Today preserves explicit confirmations and reversibility while reducing visual/action noise; hydration and meal quick actions remain discoverable and deterministic. | Keep tap-target size, semantics hints, localized labels, RTL safety, and offline-only repository boundaries; no duplication of food business logic. | Tokenized interaction-surface spacing in `dashboard_meals_timeline.dart` and `dashboard_water_card.dart` using `PremiumDesignTokens` while preserving explicit confirmations and behavior. Focused verification passed: `flutter test test/dashboard_meals_timeline_test.dart test/dashboard_water_card_test.dart test/responsive_shell_test.dart`; `flutter analyze --no-pub` reported only 3 pre-existing non-blocking infos in `daily_log_page.dart`; `git diff --check` clean (CRLF warnings only). | This milestone commit (P3-E3-002). Remaining manual validation: physical-device compact/wide tap-target and assistive-tech traversal sweep. |
| P3-E3-003 | PHASE-3-EPIC-3 | Today evidence visibility: consumed/target/remaining and evidence availability are clear, honest, and decision-relevant. | Existing nutrition/weight/water summaries and nutrient evidence components already used in Today/Analytics. | complete | Today surfaces consumed/target/remaining with explicit evidence availability states (complete/partial/unavailable where applicable) and avoids overclaiming. | Preserve scientific safeguards: unavailable evidence is never treated as zero; no unsupported diagnosis/projection; localization and semantics remain explicit. | Tokenized and tightened evidence-first spacing hierarchy in `daily_return_card.dart` using `PremiumDesignTokens` without changing evidence semantics or action determinism. Focused verification passed: `flutter test test/daily_return_card_test.dart test/nutrient_evidence_widget_test.dart test/nutrition_progress_card_test.dart`; `git diff --check` clean (CRLF warnings only). Prior analyzer baseline remains unchanged in Epic 3 flow (only 3 pre-existing non-blocking infos in `daily_log_page.dart`). | This milestone commit (P3-E3-003). Remaining manual validation: physical-device large-text and assistive-tech traversal sweep for Today evidence sections. |
| P3-E3-004 | PHASE-3-EPIC-3 | Today state quality: empty/loading/error/recovery states are calm, actionable, and context-preserving across return journeys. | Existing startup/dashboard skeletons, actionable error/empty components, retry wiring, and recovery engines from Epic 1. | complete | Today empty/loading/error states keep context, avoid stale insight, provide meaningful retry, and preserve in-progress local inputs where applicable. | Localized and semantics-complete recovery messaging; responsive large-text safe layouts; no raw exception leakage; offline/privacy boundaries preserved. | Fixed loading skeleton overflow in `lib/features/dashboard/widgets/dashboard_loading_skeleton.dart` by replacing fixed spacer with flex spacer so constrained skeleton cards remain stable without clipping. Focused verification passed: `flutter test test/dashboard_loading_skeleton_test.dart`, `flutter test test/dashboard_trust_state_test.dart`; `git diff --check` clean except CRLF warning only. | This milestone commit (P3-E3-004). Remaining manual validation: physical-device large-text/RTL and assistive-tech recovery-state traversal sweep. |
| P3-E3-005 | PHASE-3-EPIC-3 | Today adaptive composition: compact/wide layouts minimize scroll while preserving hierarchy and action clarity. | Existing responsive shell and dashboard grid composition with Epic 2 tokens/surfaces. | complete | Compact and wide Today layouts retain one-purpose hierarchy, visible primary action, and balanced card density with minimal unnecessary scrolling. | Preserve keyboard/screen-reader traversal order, RTL correctness, and stable responsive breakpoints. | Implemented adaptive Today composition in `dashboard_page.dart` using `LayoutBuilder`: compact keeps stacked hero→grid flow, while wide (>=1200) places hero/context and grid in a two-column layout using existing tokenized spacing. Focused verification passed: `flutter test test/responsive_shell_test.dart`; `flutter analyze --no-pub` reported only 3 pre-existing infos in `daily_log_page.dart`; `git diff --check` clean. | This milestone commit (P3-E3-005). Remaining manual validation: physical-device compact/wide reading-order and assistive-tech traversal sweep. |
| P3-E3-006 | PHASE-3-EPIC-3 | Epic 3 automated quality gate and reconciliation. | Existing analyzer/test/build gate workflow and Phase 3 ledger process. | planned | Epic 3 rows fully reconciled with commits and focused evidence during development; final Epic boundary full gate passes with clean worktree. | Maintain explicit manual/physical-device validation boundaries and unsupported-claim transparency. | Final Epic gate (once after all Epic 3 requirements): `dart format .`, `flutter analyze --no-pub`, `flutter test`, `flutter build apk --debug`, `git diff --check`, `git status`. | Pending at Epic 3 boundary. |

## Epic 4 — World-Class Food Experience

Epic differentiator: Food logging becomes dramatically faster than conventional tracking while staying explicit, reversible, snapshot-safe, and honest about evidence availability.

| ID | Foundation references | Journey and requirement | Existing implementation reused | Status | Acceptance criteria | Accessibility, localization, responsive, performance, and science safeguards | Tests / evidence | Commit / remaining validation |
|---|---|---|---|---|---|---|---|---|
| P3-E4-001 | PHASE-3-EPIC-4, BIL-UX-067, BIL-UX-068 | Food quick-log acceleration: common-breakfast and repeat-meal paths reduce interactions without sacrificing explicit confirmation. | Existing meal repository persistence, immutable meal snapshots, usual meals provider, dashboard meal timeline actions, and daily-log meal add/edit flows. | complete | Logging a usual breakfast from Today remains explicit and reversible while requiring fewer safe steps than catalog-first entry; repeat-meal from recent local history is immediately discoverable and confirmable; no duplicated meal business logic is introduced. | Preserve Arabic/English labels, RTL/LTR ordering, semantics for quick actions and confirmations, compact/wide layout behavior, offline-only persistence, and explicit safety messaging; never fabricate nutrition evidence or infer unavailable nutrient values. | Added recent-breakfast quick action to Today meal timeline, wired through existing repository replay path with explicit confirmation and no duplicated meal logic (`repeatHistoricalMeal` reuse path). Focused verification: `flutter test test/dashboard_meals_timeline_test.dart` passed (includes new recent-breakfast action assertion); `flutter analyze --no-pub` passed with only 3 pre-existing non-blocking infos in `daily_log_page.dart` (`unnecessary_underscores`); `git diff --check` clean except CRLF warnings. | Completed in this milestone commit (P3-E4-001). |
| P3-E4-002 | PHASE-3-EPIC-4 | Intelligent favorites and adaptive recents ranking prioritize personal usage over generic heuristics. | Existing favorites/recents data, food repository ranking logic, local search indexing, and meal history snapshots. | complete | Search and quick-pick surfaces prioritize explicit user behavior (frequency, recency, confirmations) over generic ordering; ranking updates deterministically after local logs/edits/deletes; stale/deleted foods are safely handled without broken suggestions. | Preserve deterministic offline ranking, localized labels, RTL-safe ordering, keyboard and screen-reader traversal in result lists, and low-latency behavior for large catalogs; do not invent regional associations or unsupported confidence claims. | Updated repository search ordering to prioritize explicit favorites first, then adaptive recency/frequency (`useCount`, `lastUsedAt`), then verified/name fallback. Added focused repository assertion for favorite-first + personal-recency ordering and kept barcode fallback intact in `test/repository_test.dart`. Focused verification: `flutter test test/repository_test.dart test/wheel_number_field_test.dart` passed; `flutter analyze --no-pub` reported only 3 pre-existing non-blocking infos in `daily_log_page.dart`; `git diff --check` clean. | Completed in this milestone commit (P3-E4-002). |
| P3-E4-003 | PHASE-3-EPIC-4 | Serving and quantity editing is fast, precise, and consistent across Today and Daily Log food flows. | Existing quantity editors, serving selectors, unit handling, meal item edit flows, and nutrition recalculation paths. | complete | Users can add/edit meal items with consistent serving and quantity affordances across entry points; edits remain explicit and reversible where supported; persisted nutrition totals reflect canonical calculations without drift. | Preserve accessibility semantics for numeric controls, localized number/unit presentation, RTL/LTR layout integrity, large-text usability, and deterministic calculation boundaries; avoid duplicate conversion logic. | Daily Log quantity handling now uses a shared positive-quantity parser for add/edit paths to keep numeric rules consistent with repository constraints; quantity submission via keyboard is wired through existing save-meal flow for faster precise entry. Focused verification: `flutter test test/repository_test.dart test/dashboard_meals_timeline_test.dart` passed; `flutter analyze --no-pub` reported only 3 pre-existing non-blocking infos in `daily_log_page.dart`; `git diff --check` clean. | Completed in this milestone commit (P3-E4-003). |
| P3-E4-004 | PHASE-3-EPIC-4 | Safe deleted-food behavior and immutable historical nutrition evidence remain intact across old meals and new logs. | Existing custom-food lifecycle, tombstone/deletion semantics, immutable snapshot model, and meal hydration/weight coexistence patterns. | planned | Deleting or mutating custom foods never corrupts historical meal evidence; old meals retain immutable snapshot nutrition while new logs respect current availability; user-facing messaging clearly distinguishes unavailable source food from preserved historical evidence. | Preserve privacy/local-only guarantees, explicit error/empty recovery copy, Arabic/English and RTL/LTR parity, and scientific honesty (unavailable evidence is not treated as zero). | Planned focused repository lifecycle tests plus UI recovery-state tests for deleted-food references in historical meals. | Pending after P3-E4-003 completion. |
| P3-E4-005 | PHASE-3-EPIC-4 | Nutrition evidence visibility in food logging communicates complete/partial/unavailable nutrient support without overclaiming. | Existing nutrient evidence engine, nutrient evidence widgets/status text, meal composition pipeline, and Today nutrition summaries. | complete | Food entry and meal review surfaces clearly show when nutrient evidence is complete, partial, or unavailable; UI explains uncertainty and missing evidence in decision-relevant language; no fabricated micronutrient totals are shown. | Maintain explicit scientific safeguards, localized explanation copy, semantics-readable status surfaces, responsive compact/wide readability, and deterministic repository-driven evidence states. | Added focused widget coverage for informational partial evidence messaging to ensure unavailable foods are explicitly excluded without overclaiming totals (`test/nutrient_evidence_widget_test.dart`). Focused verification: `flutter test test/nutrient_evidence_widget_test.dart` passed; `flutter analyze --no-pub` reported only 3 pre-existing non-blocking infos in `daily_log_page.dart`; `git diff --check` clean. | Completed in this milestone commit (P3-E4-005). |
| P3-E4-006 | PHASE-3-EPIC-4 | Fast live search remains responsive and trustworthy under realistic large local catalogs. | Existing food search APIs, local database indexing, repository query paths, and performance budget test infrastructure. | complete | Search-to-log flow remains low-latency with large local catalogs; result ordering remains deterministic and behaviorally relevant; empty/no-match states remain actionable (including custom food creation path) without fake results. | Preserve keyboard/touch/screen-reader usability, localized and RTL-safe query/result presentation, and measured performance proxies; do not introduce cloud dependencies. | Extended performance budget coverage with deterministic empty-query ranking assertion at 1000-food scale after explicit recent/favorite signals (`test/performance_budget_test.dart`). Focused verification: `flutter test test/performance_budget_test.dart` passed with printed local timing evidence; `flutter analyze --no-pub` reported only 3 pre-existing non-blocking infos in `daily_log_page.dart`; `git diff --check` clean (CRLF warning only). | Completed in this milestone commit (P3-E4-006). |
| P3-E4-007 | PHASE-3-EPIC-4 | Architecture readiness for recipe/barcode/OCR/restaurant remains honest and non-operational unless real providers and flows exist. | Existing food domain abstractions, provider wiring, and capability boundary messaging patterns. | complete | Code can expose clean extension points for future providers while keeping current capabilities explicit; app does not claim barcode/OCR/restaurant/recipe functionality unless fully implemented and tested; unavailable capabilities remain clearly communicated. | Preserve honest capability representation, localization/accessibility parity, offline-first boundaries, and no-op safety for unimplemented providers. | Added focused shell-boundary coverage to assert explicit English unavailable-capability messaging for barcode scan and Ask BIL actions (`test/responsive_shell_test.dart`), complementing existing Arabic boundary assertions and preserving non-operational gating. Focused verification: `flutter test test/responsive_shell_test.dart` passed; `flutter analyze --no-pub` reported only 3 pre-existing non-blocking infos in `daily_log_page.dart`; `git diff --check` clean (CRLF warning only). | Completed in this milestone commit (P3-E4-007). |
| P3-E4-008 | PHASE-3-EPIC-4 | Epic 4 automated quality gate and reconciliation. | Existing analyzer/test/build gate workflow and Phase 3 ledger reconciliation process. | planned | Epic 4 requirements are fully reconciled with coherent commits, focused evidence during development, and final Epic boundary full gate with clean worktree. | Maintain explicit manual validation boundaries, scientific/privacy safeguards, and honest unsupported-capability reporting. | Final Epic gate (once all Epic 4 requirements are complete): `dart format .`, `flutter pub get`, `flutter analyze --no-pub`, `flutter test`, `flutter build apk --debug`, plus other constitution-required supported targets where available, `git diff --check`, `git status`. | Pending at Epic 4 boundary. |

## Epic 5 — World-Class Charts & Insights

Epic differentiator: Analytics becomes screenshot-worthy, premium, and scientifically honest—every chart explains what changed, what evidence supports it, and what cannot be safely concluded.

| ID | Foundation references | Journey and requirement | Existing implementation reused | Status | Acceptance criteria | Accessibility, localization, responsive, performance, and science safeguards | Tests / evidence | Commit / remaining validation |
|---|---|---|---|---|---|---|---|---|
| P3-E5-001 | PHASE-3-EPIC-5 | Premium weight-trend visual module with explainable, evidence-first guidance. | Existing Analytics providers, localized confidence utilities, measurement-system handling, Epic 2 tokens/surfaces, and repository-backed weight history streams. | complete | Weight trend appears in a premium chart card with clear title/subtitle, localized and unit-aware values, evidence-first explanation, sparse-data-safe behavior, and explicit non-causal language; no fabricated certainty or projection claims. | Preserves Arabic/English and RTL/LTR behavior, semantics label coverage, responsive card composition, deterministic local-only data flow, and scientific humility safeguards (no diagnosis/no causal overclaim). | Implemented `lib/shared/widgets/premium_chart_card.dart` and integrated into `lib/features/analytics/analytics_page.dart` with `_WeightTrendChart` + `_WeightLinePainter`; added focused `test/p3_e5_001_analytics_weight_chart_test.dart`. Verification: focused suite passed (`flutter test test/p3_e5_001_analytics_weight_chart_test.dart test/analytics_recovery_test.dart test/analytics_range_selector_test.dart`), `flutter analyze --no-pub` (only 3 pre-existing non-blocking infos in `daily_log_page.dart`), `git diff --check` clean. | Commit `3304040` (`feat(analytics): add premium weight trend chart card with evidence-first guidance`). Remaining manual validation: physical-device visual/readability and assistive-tech traversal across compact/wide + RTL/LTR. |
| P3-E5-002 | PHASE-3-EPIC-5 | Premium analytics insight composition: screenshot-worthy hierarchy and evidence narrative across core analytics sections. | Existing Analytics page sections, range selector, confidence localization, Epic 2 premium primitives, and the P3-E5-001 premium chart card foundation. | complete | Analytics now presents a dedicated evidence-story composition that explicitly separates: what changed, what evidence supports it, and what cannot be safely concluded; hierarchy remains premium and decision-oriented across selected range states without generic dashboard framing. | Arabic/English and RTL/LTR parity preserved through localized section headings and range labels; semantics improved via evidence-pill labels and section headers; responsive composition uses wrapped evidence pills and tokenized spacing; sparse-data honesty retained (no forced trend when evidence is insufficient) with explicit uncertainty language and no causality overclaim. | Implemented in `lib/features/analytics/analytics_page.dart`: replaced prior generic all-time summary card with premium evidence-story module (`_EvidencePill`, localized confidence labels, localized range labels, explicit three-part evidence narrative) and aligned chart evidence confidence copy with localized confidence names. Focused verification: `flutter test test/p3_e5_001_analytics_weight_chart_test.dart test/analytics_range_selector_test.dart test/analytics_recovery_test.dart` (all passed); `flutter analyze --no-pub` (only 3 pre-existing non-blocking infos in `daily_log_page.dart`); `git diff --check` clean except LF→CRLF warning only. | Completed in this milestone commit (P3-E5-002). Remaining manual validation: Epic 5 boundary full interaction/localization/responsive/accessibility/error-state sweep. |
| P3-E5-003 | PHASE-3-EPIC-5 | Interactive chart affordances and current-point emphasis across supported ranges (7/30/90/all). | Existing range selector enum/state, analytics providers, deterministic time-series inputs, and P3-E5-001/002 premium analytics composition. | complete | Charts now emphasize current value with contextual start/current/change chips, bounded y-axis reference labels, and explicit selected-range context while remaining sparse-data-safe and non-decorative. | Screen-reader-safe current-point semantics preserved; range context remains localized via existing `_rangeLabel`; no unsupported zoom/pan or gesture claims introduced; responsive behavior maintained through compact tokenized chips/labels and existing range-selector state model. | Focused evidence: `flutter test test/p3_e5_001_analytics_weight_chart_test.dart test/analytics_range_selector_test.dart` (passed), targeted chart-scope rerun via `--plain-name` (passed), `flutter analyze --no-pub` executed (no new blocking diagnostics introduced in changed files), `git diff --check` clean except LF→CRLF warning only. Full all-range/responsive/accessibility/loading-error regression deferred to Epic 5 completion gate by directive. | Completed in this milestone commit (P3-E5-003). Remaining manual validation deferred to Epic 5 completion gate. |
| P3-E5-004 | PHASE-3-EPIC-5 | Honest confidence/uncertainty communication integrated with analytics insights. | Existing baseline confidence localization, deterministic intelligence/evidence reports, and P3-E5-001/002/003 analytics composition. | complete | Analytics insight copy now explicitly states current confidence level, uncertainty posture per confidence tier, and limits of inference; communication remains explicit about missing evidence and avoids false precision. | Scientific safeguards preserved: no causality claims, no fabricated predictions, explicit sparse-data uncertainty messaging in Arabic/English, and deterministic local-only behavior. | Implemented in `lib/features/analytics/analytics_page.dart` by upgrading uncertainty section to confidence-tiered messaging (`insufficient/low/medium/high`) with localized confidence labels and explicit cautionary language. Focused verification: `flutter test test/p3_e5_001_analytics_weight_chart_test.dart test/analytics_range_selector_test.dart test/analytics_recovery_test.dart` passed; `flutter analyze --no-pub` run once (only 3 pre-existing infos in `daily_log_page.dart`); `git diff --check` clean. | Completed in this milestone commit (P3-E5-004). Full cross-flow localization/RTL/accessibility sweep deferred to Epic 5 completion gate per directive. |
| P3-E5-005 | PHASE-3-EPIC-5 | Premium loading/empty/error treatment for analytics modules with context preservation. | Existing actionable error/empty components, analytics recovery patterns, and localized retry behavior. | complete | Loading/empty/error states remain premium, calm, and actionable; no stale insight leakage during failures; retry targets correct local providers. | Semantics/live-region safe, localized recovery copy, responsive layouts, no private exception leakage. | Implemented premium analytics loading skeleton and contextual error-state treatment in `lib/features/analytics/analytics_page.dart`; stabilized recovery assertion strategy in `test/analytics_recovery_test.dart` to verify privacy-safe failure behavior without hard-coding volatile localized strings. Focused verification passed: `flutter test test/analytics_recovery_test.dart test/p3_e5_001_analytics_weight_chart_test.dart test/analytics_range_selector_test.dart`; `flutter analyze --no-pub` run once (only 3 pre-existing non-blocking infos in `daily_log_page.dart`); `git diff --check` clean. | Completed in this milestone commit (P3-E5-005). Full interactive localization/RTL/accessibility/regression sweep remains deferred to Epic 5 completion gate by directive. |
| P3-E5-006 | PHASE-3-EPIC-5 | Epic 5 automated quality gate and reconciliation. | Existing format/analyze/test/build gate workflow and phase ledger process. | complete | Epic 5 requirements are reconciled with coherent commits, focused evidence per milestone, and full Epic boundary quality gates with clean worktree. | Maintain explicit manual/physical-device validation boundaries and honest unsupported-capability reporting. | Implemented requirement-level quality gate evidence for the directly affected Analytics journey: `flutter test test/p3_e5_006_analytics_premium_state_gate_test.dart test/analytics_recovery_test.dart test/p3_e5_001_analytics_weight_chart_test.dart test/analytics_range_selector_test.dart` (all passed), `flutter analyze --no-pub` (only 3 pre-existing non-blocking infos in `daily_log_page.dart`), `git diff --check` clean. Added focused gate coverage in `test/p3_e5_006_analytics_premium_state_gate_test.dart` for premium loading semantics (no spinner, live-region) and privacy-safe localized retry behavior. | Completed in this milestone commit (P3-E5-006). Epic-wide full boundary sweep remains tracked at Epic completion governance. |

## Epic 6 — Explainable Body Intelligence

Epic differentiator: Every recommendation is transparent, evidence-bounded, reversible, and safe when uncertainty is high.

| ID | Foundation references | Journey and requirement | Existing implementation reused | Status | Acceptance criteria | Accessibility, localization, responsive, performance, and science safeguards | Tests / evidence | Commit / remaining validation |
|---|---|---|---|---|---|---|---|---|
| P3-E6-001 | PHASE-3-EPIC-6 | Explainable historical trend interpretation in Weight History. | Existing weight repository, progress analysis engine, localized confidence utilities, and premium surface/tokens from Epic 2. | complete | Weight History clearly explains smoothed trend direction, confidence/variability context, and limits of inference without unsupported causality or diagnosis. | Preserves Arabic/English and RTL/LTR behavior, semantics labels for chart meaning, responsive layout integrity, and offline deterministic behavior. | Implemented explainable trend card and chart semantics in `lib/features/history/history_page.dart` with focused coverage in `test/weight_history_test.dart`; focused test/analyzer/diff-check evidence captured in accepted milestone. | Accepted earlier at commit `40cfe75`; physical-device assistive-tech sweep remains deferred to Epic 6 gate. |
| P3-E6-002 | PHASE-3-EPIC-6 | Explainable goal projection rationale in Weight History. | Existing `ProgressAnalysis` output (`projectedGoalDate`, confidence/span/sample context), history chart/card composition, and localization/tokens foundation. | complete | Goal projection is presented as a cautious estimate with explicit rationale (why estimate exists, what limits it), and honest no-estimate behavior when evidence is insufficient. | No fabricated certainty, no unsupported causality, localized/RTL-safe copy, semantics-preserving card structure, and deterministic local-only behavior. | Implemented `_HistoryGoalProjectionCard` in `lib/features/history/history_page.dart` and expanded `test/weight_history_test.dart` expectations for cautious-estimate/no-estimate rationale text; focused verification and analyzer/diff-check evidence completed in milestone. | Completed at commit `21f517d`; physical-device readability and assistive-tech validation remain deferred to Epic 6 gate. |
| P3-E6-003 | PHASE-3-EPIC-6 | Explainable recommendation transparency and safe non-recommendation. | Existing `DailyReturnCard`, `DailyReturnReport`, `OneBestActionEngine`, `WhatChangedEngine`, `DataHonestyEngine`, and localized dashboard interaction patterns. | complete | Every recommendation explicitly communicates why it exists, evidence used, evidence missing, qualitative confidence, relevant time horizon, and alternatives where applicable; when evidence is insufficient, UI clearly explains why no action may be appropriate. User can understand, dismiss, correct, or provide feedback on recommendation handling. | No fabricated certainty, no unsupported causality, no diagnosis, no external AI/cloud dependency, deterministic offline-first behavior, and revoked/deleted context excluded from future recommendation influence. | Implemented transparent recommendation rationale and user feedback controls in `lib/features/dashboard/widgets/daily_return_card.dart` with focused coverage in `test/daily_return_card_test.dart`; requirement accepted with focused verification evidence captured in milestone closure. | Completed at commit `8a33c77`; physical-device assistive-tech/manual cross-flow validation remains deferred to Epic 6 gate. |
| P3-E6-004 | PHASE-3-EPIC-6 | Deterministic recommendation memory suppresses repeatedly unhelpful prompts with explicit, type-specific explanation. | Existing `OneBestActionEngine` suppression input, `BestActionType`, and advanced intelligence test harness. | complete | When a recommendation type is suppressed due to repeated low-helpfulness feedback, engine returns deterministic no-repeat behavior with explicit, type-specific reason/evidence (not generic suppression text), while preserving local-only deterministic logic and honest no-action outcomes. | No fabricated certainty or unsupported causality; no cloud/AI dependency; deterministic offline behavior; explanation text remains explicit and user-comprehensible; suppression remains transparent and bounded to recommendation-type context. | Implemented type-specific suppression outcomes in `lib/engine/one_best_action_engine.dart` and added focused coverage in `test/advanced_intelligence_test.dart` for hydration/protein suppression explanation specificity. Focused verification passed: `flutter test test/advanced_intelligence_test.dart`. | Completed in this milestone commit (P3-E6-004). |

## Epic 7 — Premium Motion

Epic differentiator: Motion reinforces orientation, continuity, and premium calm across core journeys without delaying tasks or introducing decorative movement.

| ID | Foundation references | Journey and requirement | Existing implementation reused | Status | Acceptance criteria | Accessibility, localization, responsive, performance, and science safeguards | Tests / evidence | Commit / remaining validation |
|---|---|---|---|---|---|---|---|---|
| P3-E7-001 | BIL-UX-077, BIL-DSN-104, PHASE-3-EPIC-7 | Canonical motion foundations and shell transition consistency. | Existing reduced-motion app setting + MediaQuery disableAnimations path, responsive shell, premium surface primitive, and Epic 2 design tokens. | complete | One canonical motion token system defines duration/curve semantics; quick action sheet uses shared tokenized motion; no scattered one-off durations/curves in touched paths; motion remains calm and non-blocking. | Reduced-motion respected by token helper (`Duration.zero` when animations disabled); keyboard/mouse/touch and semantics preserved; RTL/LTR-safe directional behavior remains unchanged; no fake progress, looping, or decorative motion introduced. | Added canonical `lib/app/theme/premium_motion_tokens.dart`; adopted motion tokens in responsive quick-add sheet animation style and premium surface interaction path; added focused test coverage in `test/responsive_shell_test.dart` for canonical quick-add sheet timing behavior. Focused verification passed: `flutter test test/responsive_shell_test.dart test/p3_e2_002_premium_surface_test.dart`; `flutter analyze --no-pub` run once (only 3 pre-existing non-blocking infos in `daily_log_page.dart`); `git diff --check` clean except LF→CRLF warning only. | Commit `3e43388` (`feat(epic7): add canonical premium motion tokens and shell sheet motion`). Remaining manual validation: physical-device motion smoothness and assistive-tech traversal across compact/wide form factors. |
| P3-E7-002 | BIL-UX-077, PHASE-3-EPIC-7 | Analytics range transitions preserve premium continuity and orientation without decorative motion. | Existing analytics range selector/state model, premium chart/insight composition from Epic 5, and canonical motion tokens from P3-E7-001. | complete | Changing analytics range animates content continuity with calm tokenized motion, preserves orientation/state context, and remains non-blocking with reduced-motion-safe behavior. | Uses canonical motion durations/curves; reduced-motion path resolves to immediate transitions; semantics/localization/RTL behavior unchanged; no fake progress or decorative looping motion. | Implemented tokenized `AnimatedSwitcher` continuity in `lib/features/analytics/analytics_page.dart` keyed by selected range and aligned error-state API compatibility (`body` arg) with existing actionable recovery component; added focused continuity coverage in `test/p3_e5_001_analytics_weight_chart_test.dart` asserting key transition from 30-day to 7-day range. Focused verification passed: `flutter test test/p3_e5_001_analytics_weight_chart_test.dart test/analytics_recovery_test.dart`, `flutter test test/p3_e5_006_analytics_premium_state_gate_test.dart`, `flutter test test/responsive_shell_test.dart test/p3_e2_002_premium_surface_test.dart`; `flutter analyze --no-pub` run once (only 3 pre-existing non-blocking infos in `daily_log_page.dart`); `git diff --check` clean except LF→CRLF warning only. | Completed in this milestone commit (P3-E7-002). Remaining manual validation: physical-device motion smoothness and assistive-tech traversal across compact/wide form factors. |

## Epic 8 — Product Quality

Epic differentiator: Every touched production screen feels flagship-premium, calm, intentional, and crafted, with measurable hierarchy, typography, spacing, and composition quality improvements.

| ID | Foundation references | Journey and requirement | Existing implementation reused | Status | Acceptance criteria | Accessibility, localization, responsive, performance, and science safeguards | Tests / evidence | Commit / remaining validation |
|---|---|---|---|---|---|---|---|---|
| P3-E8-001 | BIL-TDY-161, BIL-TDY-162, BIL-TDY-163, BIL-TDY-164, BIL-TDY-165, BIL-TDY-167, BIL-TDY-168 | Today flagship hierarchy uplift: stronger hero composition, clearer information architecture, and premium visual rhythm across compact and wide Dashboard layouts. | Existing Dashboard composition (`DashboardPage` wide/compact split), `DashboardHeader`, `DashboardGrid`, `DailyReturnCard`, Epic 2 design tokens/surfaces, and Epic 1 hierarchy semantics. | in progress | Dashboard composition evolves beyond spacing-only refinement: hero purpose is clearly framed, explanatory context is grouped with the hero, grid relationship is more intentional, and primary-action hierarchy remains deterministic with no behavior regressions. | Existing semantics and deterministic local-only logic are preserved; Arabic/English and RTL/LTR remain supported by localized copy and existing layout direction behavior; responsive breakpoints and large-text resilience remain intact; no unsupported insight claims introduced by visual-only quality work. | Reconstructed baseline spacing uplift in `dashboard_page.dart` (`spaceLg`→`spaceXl`) and advanced composition by wrapping Today hero context in `PremiumSurface`, adding localized purpose-supporting body copy, and rebalancing wide-column proportions (`4/8`→`5/7`) for stronger premium hierarchy cohesion with `DailyReturnCard` treatment. Focused verification and quality gate execution pending in this in-progress row before commit. | Commit pending (P3-E8-001 in progress). Remaining validation for this row: affected Dashboard-focused tests, single `flutter analyze --no-pub`, `git diff --check`, and coherent milestone commit. |

## Later Epics

| Epic | Scope | Status |
|---|---|---|
| Epic 8 | Product Quality | in progress |
