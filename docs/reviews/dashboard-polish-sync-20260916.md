# Dashboard polish and sync review — 2026-09-16

## النطاق والحالة

- Worktree: `G:\BIL_Project\body_intelligence_log_dashboard_polish`
- Branch: `codex/dashboard-polish-20260916`
- Base: `66172a802f1897abad95e9e08828ac79ba5b5227`
- التعديلات محلية غير مرفوعة، ولم يُنشأ commit أو build أو إصدار متجر في هذه المهمة.
- شجرة الإصدار `body_intelligence_log_google_play_remediation` وشجرة العمل الأصلية لم تتغيرا بهذه المهمة.

## التغييرات المنفذة

1. بوليش أحجام ومسافات فقط للعنوان، AI Coach، الساعة، بطاقات Weight/Meals/Water، مخطط الاتجاه، Body Twin، وDiscover. بقيت الأسماء والصور والوجهات والبيانات كما هي.
2. Calories هي البداية في شريط التغذية حتى لو كان التفضيل الغذائي المحفوظ Heart healthy. حماية Premium الزجاجية باقية.
3. إزالة حشوة أسفل Dashboard الإضافية: كانت 176 على الهاتف و132 على الشاشات الكبيرة، مع أن غلاف التطبيق يحجز مساحة التنقل أصلًا. الفاصل الأخير الآن 16.
4. استعادة موضع التمرير إذا بقي خارج الحدود بعد انتهاء الحركة أو تغيير حجم المحتوى. لا يُثبَّت السحب أثناء انتظار المزامنة ولا تُستبدل الحركة الأصلية للمنصة.
5. تثبيت اشتراك `dashboard.preset` ومتحكم التمرير الأفقي. اختيار LOW CARB لا يزيل القائمة ويعيد إنشاءها أثناء الحفظ، ولا يرجع إلى CALORIE FOCUSED. اختُبر النجاح والفشل بالعربية والإنجليزية.
6. تثبيت صفحة مخطط الوزن/الخطوات أثناء تحديث البيانات، والتخلص من متحكم الصفحة عند إغلاق الواجهة.
7. إبقاء مساحة كافية لتفسير السعرات المحروقة، والسماح لبطاقة Body Twin بالتمدد مع النص الطويل أو تكبير الخط.

## نتيجة مراجعة المزامنة

لم يُعدَّل أي كود أصلي للمزامنة أو أذونات أو حسابات غذائية في هذه المهمة؛ التغيير الوحيد داخل Connected Health هو حجم الساعة المعروضة.

- iOS: `readDailyTotals` ما زال يستخدم HealthKit لتجميع الخطوات والمسافة و`activeEnergy` بوحدة kcal. نطاق الأنواع الصحية الأخرى في قراءة Apple Health لم يتغير.
- Android: الكود الحالي، قبل هذا البوليش، يقصر Health Connect على الخطوات والمسافة والسعرات النشطة لتلبية نطاق الصلاحيات الأدنى. النوم وتركيب الجسم والنبض والتغذية والكتابة ليست ضمن صلاحيات Health Connect لهذه النسخة؛ هذا ليس فقدًا أحدثه البوليش.
- اختبارات القناة الأصلية المحاكية تتبع 321 kcal حتى محدد بيانات Dashboard، وتثبت عدم مضاعفة القراءة المتكررة، وعدم استعمال سعرات أمس لليوم، والحفاظ على آخر قراءة عند فشل التحديث.
- اختبار iOS يثبت بقاء الوزن والنبض والنوم المخزّنة أثناء تحديث النشاط الخفيف. السجلات الخام لا تُحذف.
- عرض السعرات المحروقة لا يعني إضافتها تلقائيًا إلى ميزانية الأكل؛ هذا يبقى تابعًا لإعداد المستخدم الموجود أصلًا.

هذه أدلة كود واختبارات محلية، وليست قياسًا مباشرًا لمزامنة هاتف أو ساعة فعلية.

## التحقق الأولي من الداشبورد والمزامنة

- اختبارات Dashboard والبوليش والتنقل والتحرير والاستجابة: **226 ناجحة**.
- اختبارات المزامنة والصلاحيات والخطوات وحساب السعرات: **70 ناجحة**.
- مجموع الجولة الأولى: **296 اختبارًا ناجحًا**، وليس جميع اختبارات المشروع.
- `dart analyze --fatal-infos` لكل ملفات Dart المعدلة والجديدة: **No issues found**.
- `git diff --check`: ناجح.
- اختبارات التخطيط الجديدة: العربية والإنجليزية، عرض 320 و390 و430، خط 100% و160%. الاختبارات القائمة تغطي كذلك ترجمات الواجهة الـ25 والشاشات اللوحية.
- سجلات محلية: `build/dashboard_polish_review/dashboard_tests.log` و`analyze.log` و`health_tests.log`.

## متابعة صور الأيقونات IMG_8509–IMG_8512

السبب المؤكد في الكود: الثيم المستخدم في `main.dart` يحدد `ListTileThemeData.horizontalTitleGap` بصفر. بعض الصفحات كانت تضيف فاصلًا محليًا، لكن Customize Today وBody context لم تضيفاه. كذلك كانت شبكة التخصيص تفرض عمودين بارتفاع ثابت، رغم عدم كفاية العرض للاسم والأيقونة ومفتاح التشغيل معًا.

- المسافة الافتراضية بين أيقونة صف القائمة ونصه أصبحت 12 نقطة تخطيط، مع دعم الاتجاهين. لم يتغير قاموس الأيقونات أو ألوانه أو رموزه.
- أيقونات العروض الجاهزة وBody context وأهداف التغذية: حاوية 32 بدل 40، ورمز 18 بدل 22. بطاقات التخصيص: حاوية 30 بدل 36 ورمز 18 بدل 20.
- بطاقات التخصيص تتكيف مع العرض وتكبير الخط: عمود واحد على الهاتف، وعمودان عندما يتوفر عرض كافٍ. النص يلتف دون قص؛ مفاتيح التشغيل الأصلية ومساحات اللمس محفوظة. ارتفاع بطاقات العروض الجاهزة يستوعب تكبير الخط.
- الأسماء والترجمات وترتيب الخيارات والأذونات والاستحقاقات ومسارات التنقل وحفظ البيانات لم تتغير.
- اختبارات التباعد والتخصيص والتوجيه والثيم والعقود المرتبطة: **108 ناجحة**، منها 64 حالة هندسة وتفاعل جديدة تغطي iOS وAndroid، العربية والإنجليزية، عروض 320/390/430/600، وخط 100% و160%.
- إعادة فحص الصفحات الأساسية السبع وسجل الطعام وتفاصيله بـ25 لغة وخط 160%: **52 ناجحة**.
- مجموع هذه المتابعة: **160 اختبارًا ناجحًا**. يتداخل بعضها مع الجولة الأولى، لذلك لا تجمع الأعداد باعتبارها اختبارات فريدة أو فحصًا شاملًا للمشروع.
- التحليل لكل ملفات Dart المعدلة والجديدة في شجرة البوليش (23 ملفًا): **No issues found**.
- سجلات المتابعة: `icon_focused_tests.log` و`icon_locale_regression.log` و`icon_analyze.log` داخل `build/dashboard_polish_review/`.
- تمت معاينة صور محلية للصفحتين باللغتين، مولدة من Widgets الإنتاج بخطوط اختبار حقيقية. هذه ليست لقطات من iPhone أو Android فعليين؛ الرموز الأصلية للمنصة قد تستخدم البدائل في بيئة الاختبار.
- بقيت شجرة `body_intelligence_log_google_play_remediation` نظيفة على `66172a8`؛ لم ينشأ commit أو build أو رفع من هذه المتابعة.

## متابعة وميض Custom cards والمعاينة الحالية

السبب المؤكد محليًا: حفظ مفتاح بطاقة واحدة كان يشغّل `_saving` العام؛ فيتعطل كل مفتاح وزر في صفحة Customize Today ثم يعود، ويظهر شريط تقدم عام. هذا يغير ألوان الصفحة كلها عند كل ضغطة.

- فصل عرض الحفظ الفردي عن الحفظ الجماعي. عند تبديل بطاقة يظهر مؤشر صغير على أيقونتها فقط، دون تعتيم بقية الصفحة أو شريط تقدم عام.
- منع الخروج والكتابات المتعارضة أثناء الحفظ باقٍ داخل المعالجات. القيمة المعروضة تتبع الحفظ الفعلي، ويظهر خطأ قابل لإعادة المحاولة عند فشله؛ لا يُعرض نجاح غير محفوظ.
- بقاء عنصر البطاقة وموضعها والتمرير ثابتة أثناء الحفظ وبعده، مع الحفاظ على إصلاح ثبات قائمة العروض الجاهزة.
- **50 اختبارًا ناجحًا** للتخصيص والثبات: منها 36 حالة جديدة لكل البطاقات التسع، بالعربية والإنجليزية، في النجاح والفشل، مع محاكاة قرص بطيء ونقرات متكررة ومحاولة الخروج أثناء الحفظ.
- **64 اختبار تباعد وتفاعل ناجحًا** على iOS وAndroid بعروض 320/390/430/600 وخط 100% و160%، بعد إضافة مؤشر البطاقة.
- **اختبارا معاينة ناجحان** للداشبورد الفعلي وشريط التنقل، بالإنجليزية والعربية. مجموع التحقق لهذه المتابعة 116 حالة، وليس كامل اختبارات المشروع.
- سجلات: `custom_card_stability.log`، `current_preview_and_icons.log`، `current_preview.log` داخل `build/dashboard_polish_review/`. فشل اختبارا المعاينة في المحاولة الوسطية بسبب عدم إعادة إعداد الظلال الخاص بالاختبار قبل فحص التنظيف؛ صُحّح التنظيف وأعيد الاختباران بنجاح. اختبارات التباعد الـ64 نجحت في تلك المحاولة.

المعاينة تستخدم `DashboardPage` و`ResponsiveAppShell` الإنتاجيين، قاعدة ذاكرة واشتراكًا ومصدر صحة محاكيين، وليست لقطة من حساب المستخدم أو هاتفه. الأرقام (87 kg، 108 خطوة، 75 bpm، 321 kcal) بيانات اختبار، وليست دليل مزامنة فعلية. استُخدمت خطوط المعاينة بدل خطوط النظام في بيئة Flutter الرأسية، بما فيها بديل خط زر Today؛ لم تتغير خطوط الإنتاج لهذا الغرض.

الصور المولدة:

- `build/dashboard_polish_review/dashboard-current-phone-en.png` و`dashboard-current-phone-ar.png`: مساحة هاتف 430×932 نقطة تخطيط، مع شريط التنقل الحقيقي.
- `build/dashboard_polish_review/dashboard-current-full-en.png` و`dashboard-current-full-ar.png`: نفس صفحة الإنتاج على لوحة ممتدة بعرض الهاتف لإظهار كامل المحتوى. ليست صورة مولدة بالذكاء الاصطناعي أو تصميمًا تصوريًا.

**نتيجة المعاينة الأولى، قبل استكمال الترتيب:** أظهرت أن العنوان كان على سطرين، والساعة بالمنتصف والملخص تحتها. لذلك لم تكن التغييرات الأولى مطابقة للترتيب المطلوب. طلب المستخدم تطبيق المرجع فعليًا، ونُفّذ الاستكمال التالي في نفس شجرة البوليش.

## استكمال ترتيب المرجع بناءً على تأكيد المستخدم

- هيدر واحد مدمج على الهاتف الاعتيادي؛ الشعار أكبر ويملأ المساحة بين صورة الحساب والجرس بفاصل 6 من الجهتين. استُبدلت كلمة Edit، بطلب المستخدم، بأيقونة إعدادات صغيرة داخل دائرة 28 مع مساحة لمس 44 وتلميح مترجم. لا تتغير وظيفتها أو وجهتها. الشاشات الضيقة والخط الكبير تستخدم سطرين لمنع التداخل.
- AI Coach بطاقة واحدة، دون إطار خارجي ثانٍ، مع مدخل محادثة وأيقونات ومسافات أصغر. النصوص والوجهة لم تتغير.
- الساعة بجانب عمود Weight/Meals/Water في بطاقة Fitness snapshot واحدة، مع آخر مزامنة في رأس البطاقة. يظهر الوزن المحفوظ لليوم من مصدر الوزن الحالي، ولا يُنشأ رقم بديل أو قراءة صحية جديدة.
- بطلب المستخدم الأخير أصبحت بطاقات الوزن والوجبات والماء بعرض محتواها، ومحاذاة فعلية إلى يمين الساعة حتى بالعربية، دون تمدد أو فراغ زائد داخل يمين البطاقة. ارتفاعها يتبع المحتوى مع بقاء مساحة اللمس 48 على الأقل في المعاينة.
- عند تكبير الخط أو ضيق العرض يرجع التجميع إلى ترتيب رأسي مقروء بدل ضغط المحتوى. الاستدعاءات الصحية والبيانات وأزرار تفاصيل الخطوات والنبض والسعرات كما هي.
- بطاقة الوزن: الرقم واضح بجانب مخطط صغير؛ القياس الوحيد نقطة واحدة، وليس اتجاهًا وهميًا. سجل الخطوات يحتفظ بمخطط الأعمدة المبدوء بالصفر.
- تصغير Body Twin وصور Discover وأسهمها وشريط التنقل السفلي، مع بقاء Community بعرض كامل. لا أسماء جديدة ولا وظائف أو وجهات جديدة.
- Calories بقيت أول بطاقة تغذية، مع معادلة السعرات وملاحظة المحروق وحماية البطاقات المدفوعة كما هي.
- أُعيد توليد صور `dashboard-current-*` من هذا الكود بعد الاستكمال، وليست صور المرحلة السابقة.
- فحص التخطيط باللغتين وعروض 320/390/430 والخط 100%/160%، وثبات السحب عند الطرفين: **16 حالة ناجحة** في `reference_layout.log`. تتضمن اختبارات المعاينة تأكيد الهيدر المدمج والتوسيط ومساحة لمس Edit ووجود الساعة والملخص جنبًا إلى جنب، وظهور السعرات المحروقة.
- بعد ضبط الشعار وأيقونة الإعدادات والبطاقات ذات العرض التلقائي: **83 اختبارًا ناجحًا** في `reference_final_header.log`، تشمل المعاينة الإنتاجية باللغتين، تباعد الشعار وأزراره، وجهة أيقونة الإعدادات، و25 لغة مع تكبير الخط. فشل فحص التلامس في محاولة الهيدر الأولى؛ أُصلح الفصل بين الشعار والجرس وأعيدت جميع اختبارات الهيدر بنجاح.
- نجحت كذلك 12 حالة تخطيط متعددة الأحجام والخطوط قبل آخر تعديل عرض البطاقات والهيدر (`reference_final_layout.log`)، و16 حالة وصول لشريط التنقل في `reference_navigation.log`. ليست هذه نتائج كامل اختبارات المشروع، ولا تُجمع الجولات المتداخلة كاختبارات فريدة.
- تحليل 29 ملف Dart معدلًا وجديدًا: **No issues found**؛ وأعيد تحليل ملفات آخر تعديلات عرض البطاقات والهيدر على حدة. بقي العمل محليًا دون commit أو build أو نشر.

## المتبقي على جهاز فعلي بعد بناء نسخة تحتوي هذه التعديلات

### متابعة تمركز الشعار والأيقونات عند الأطراف

- أصبح الشعار في منتصف عرض الشاشة الفعلي، لا في منتصف المسافة المتبقية بين أزرار غير متساوية. يستعمل الهيدر عرض المنطقة الآمنة كاملًا، مع بقاء هوامش البطاقات الأخرى كما كانت.
- البروفايل عند الطرف الأيسر، والجرس وأيقونة التخصيص عند الطرف الأيمن، في العربية والإنجليزية. أُزيلت المساحة الافتراضية الإضافية حول زر الجرس مع حفظ مساحة لمس 44×44 وعدم تداخلها مع الشعار.
- لم تتغير وجهات الأزرار أو ترتيب إشعار موافقة المزامنة أو البيانات والبطاقات.
- **16 اختبارًا ناجحًا** بعد هذا التعديل (`centered_edge_header.log`): المعاينة باللغتين، التوسيط الحقيقي وتباعد الأزرار على أحجام الهاتف والتابلت، وارتداد السحب على iOS/Android. أُعيد توليد الصور من الكود النهائي.
- التحليل للملفات الخمسة المتأثرة: **No issues found**. لا build أو نشر.

1. مقارنة السعرات النشطة لليوم مع Apple Health أو Health Connect، ثم تحديثها مرتين والتأكد من عدم التضاعف.
2. سحب Dashboard عند الطرفين، بما في ذلك أثناء مزامنة بطيئة، والتأكد من الرجوع وعدم وجود فراغ كبير أسفل Discover.
3. اختيار LOW CARB في Edit والتأكد من ثبات مكانه وظهور الاختيار، ثم إعادة فتح الصفحة للتحقق من الحفظ.
4. مراجعة الأحجام على iPhone وAndroid مع اللغة العربية وتكبير الخط.
5. تبديل كل Custom card والتأكد من غياب وميض الصفحة على الجهاز، ثم إعادة فتح Customize Today للتحقق من استمرار الحفظ.

نسخة iOS 18 المثبتة سابقًا لا تحتوي هذه التعديلات المحلية؛ إعادة تجربتها لا تختبر البوليش الجديد.
