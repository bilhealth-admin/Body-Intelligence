/// Stable semantic identifiers for the dashboard's composed regions.
///
/// These identifiers are intentionally presentation-only. They allow layout
/// policy and tests to describe ordering without importing widgets or health
/// intelligence types.
enum DashboardSection { dailyCommand, progress, analyticsCenter, bodyIdentity }
