import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Primary
  static const Color primary = Color(0xFF2563EB);
  static const Color secondary = Color(0xFF06B6D4);

  // Background
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);

  // Status
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color danger = Color(0xFFEF4444);

  // Nutrition
  static const Color calories = Color(0xFFFF6B35);
  static const Color protein = Color(0xFF16A34A);
  static const Color carbs = Color(0xFF3B82F6);
  static const Color fats = Color(0xFFFACC15);

  // Water
  static const Color water = Color(0xFF0EA5E9);
}